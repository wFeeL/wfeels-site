import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';

/** Приёмка задания «Откатить подсветку соседних кадров на страницах кейсов»
 *  (2026-08-28). Три измеренных довода к откату:
 *   1) приём не решает задачу «рассмотреть экспонат» — подкадры показаны в
 *      масштабе 0.21/0.27, приглушение соседей не прибавляет наведённому
 *      кадру ни одного пикселя;
 *   2) приём нарушал критерий брифа 10.5 (AA для всех текстовых пар) —
 *      контраст подписи под кадром падал 5.38:1→2.25:1 (светлая тема) и
 *      6.96:1→2.95:1 (тёмная) при пороге 4.5;
 *   3) в панели из трёх наведение на мелкий подкадр (0.21) гасило самый
 *      крупный кадр (0.43) — жест указывал на мелкое и тушил крупное.
 *
 *  Сторож механический и структурный: в `<style>` компонента `:hover` не
 *  встречается вовсе. Это не проверка конкретного правила (`.shots.trio`
 *  и т.п.), а запрет самого приёма, чтобы любая будущая попытка вернуть
 *  подсветку через другой селектор тоже падала здесь, а не только в тексте
 *  задания. */

const SOURCE = readFileSync(
  new URL('./CaseSpread.astro', import.meta.url),
  'utf8',
);

describe('CaseSpread.astro — без hover-подсветки соседних кадров (откат 2026-08-28)', () => {
  it('в исходнике компонента :hover не встречается вовсе', () => {
    expect(SOURCE).not.toContain(':hover');
  });
});
