<div class="prose"><slot /></div>

<style>
  /* 50ch, не 68ch: при базовом кегле текста (18px, `body` html — `styles/
     base.css`) 68ch давал строку ≈60 высот прописной буквы — заметно выше
     цели 42–45 (замер ревью 2026-08-13, часть 3 задания «Контейнер обратно к
     1180»). Узкая колонка для текста, а не узкий контейнер для всего —
     контейнер секции (`--container`) остаётся 1180px, сужается только сама
     колонка прозы. 50ch измерено в браузере как середина цели — 44,3 высоты
     прописной у самого длинного абзаца сайта (`home/Pain.astro`). */
  .prose { max-width: 50ch; }
  .prose :global(h2) {
    font: 800 clamp(1.375rem, 3vw, 2rem)/1.15 var(--font-head);
    letter-spacing: -0.02em;
    margin: 48px 0 16px;
  }
  .prose :global(h3) {
    font: 700 1.125rem/1.3 var(--font-head);
    margin: 32px 0 12px;
  }
  .prose :global(p) { margin: 0 0 16px; line-height: 1.65; }
  .prose :global(ul), .prose :global(ol) { margin: 0 0 16px; padding-left: 24px; }
  .prose :global(li) { margin-bottom: 8px; }
  .prose :global(a) { color: var(--accent); }
  .prose :global(code) {
    font: 400 0.875em/1 var(--font-mono);
    background: var(--accent-soft);
    border-radius: 4px;
    padding: 2px 5px;
  }
  .prose :global(hr) { border: 0; border-top: 1px solid var(--border); margin: 40px 0; }
</style>
