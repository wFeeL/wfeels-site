/** Реестр путей линии на фоне главной — бриф `70-workshop/specs/site-v3/
 *  05-line.md`, раздел 10, шаг 4: «Нарисовать одиннадцать путей по
 *  назначению 4.3, шаблону 4.4 и карте перекрытия 6.2. Рисуются руками,
 *  по одному, каждый — со снимком своей секции». Раздел 10, шаг 6:
 *  «Подключить реестр к странице» — `Section.astro` и `Footer.astro`
 *  читают `LINE_PATHS` напрямую, второго источника геометрии в кодовой
 *  базе больше нет (`backgroundLine.ts` считает только сторону/переход,
 *  метаданные Ч-4, не `d`).
 *
 *  До этой правки файл нёс ВТОРОЙ слой, `FALLBACK`, — механический
 *  пересчёт геометрии шага 2 для секций, ещё не нарисованных вручную.
 *  Он снят: `HAND_DRAWN` ниже покрывает все десять секций главной
 *  (раздел 10 шаг 4 завершён полностью), второй геометрии в системе не
 *  остаётся ни для одной записи. Появится одиннадцатая секция без своего
 *  пути — `LINE_PATHS[id]` будет `undefined`, и потребитель
 *  (`Section.astro`) обязан молча пропустить рисунок для неё, а не читать
 *  чужую геометрию (раздел 8: «секция без данных рисует прямую нитку в
 *  левом поле» — отдельная работа, не эта правка).
 *
 *  ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/11-line-narrator-brief.md`,
 *  раздел 12.1, В-4): запись `footer` была СНЯТА. Владелец подтвердил
 *  референс — линия уходит за ЛЕВУЮ кромку холста в `contact` (`x = −200`)
 *  и в подвал не заходит; «дотягивание линии до подвала» отменялось.
 *
 *  ПРАВКА `2026-08-27`, тем же днём, позже (`70-workshop/specs/site-v3/
 *  16-line-digits-and-finale-brief.md`, раздел 3.3, вариант Б «Разгон»,
 *  выбран владельцем): В-4 ПЕРЕОТКРЫТА решением владельца. Запись `footer`
 *  ВОЗВРАЩЕНА — уход за левую кромку («жест не новый») переезжает с
 *  `contact` на подвал, тем же размером бокового хода (25,9%, Г-4
 *  тождественно). `contact.wide` становится чистым доком без ухода
 *  (боковой ход 0); подвал наследует уход и заканчивает его окончательно —
 *  краска покидает холст на 36 px выше низа документа (было 415 px тишины
 *  у варианта, где путь кончался в `contact`). Голова доводится до `100vh`
 *  на последних `4Δ` прокрутки (`BackgroundLine.astro`, `line-finish`) —
 *  без этого разгона хвост ухода в подвале физически не дорисовывался бы:
 *  недостижимая полоса низа документа (`min(0,2·vh, 347px)`) больше самого
 *  ухода при `vh ≥ 1500`. Фон подвала (`footer::before`, D-126, `z-index:
 *  −2`) остаётся в силе НЕИЗМЕННО — он и даёт краске подвала пол, на
 *  котором она лежит; это разные решения, ни одно не отменяет другое.
 *  Реестр снова несёт ОДИННАДЦАТЬ записей — десять секций главной плюс
 *  подвал.
 *
 *  Помощники (`straightPath`, `traversePath`) — не общая формула вместо
 *  рисунка, а инструмент того же рода, что нёс прежний `mirrorD` в
 *  `backgroundLine.ts`: каждый вызов — решение, куда идёт путь ИМЕННО этой
 *  секции; числа (доки, амплитуда) подобраны рукой под карту перекрытия
 *  конкретной секции и проверены контракт-тестом плюс снимком на живой
 *  странице, а не выведены единой формулой на всех.
 *
 *  `narrow` — мобильная нитка (< 900 px, раздел 8): всегда левый док, без
 *  событий — раздел 8 «секция без данных рисует прямую нитку в левом поле»,
 *  тот же приём здесь для КАЖДОЙ секции: мобильная ширина ниже порога 900 px
 *  (раздел 6), на котором вообще стоит рисунок, событий там нет по Г-4
 *  («промежуточных значений нет»). Пока не подключена к разметке (раздел 10
 *  шаг 6 подключает только `wide`, порог видимости остаётся прежним, 480 px)
 *  — отдельная работа шага 6 плана, не эта правка.
 */
import { computeVbH, MEASURED_FOOTER_HEIGHT, MEASURED_SECTION_HEIGHT } from './backgroundLine';

/** Толщина штриха линии в единицах `viewBox` (раздел 7.2 брифа `05-line`):
 *  `stroke-width: 34` в `BackgroundLine.astro`. Единственный источник числа
 *  для контракт-теста (Г-1: `R_min ≥ 8·w`) — второй раз оно не набрано;
 *  сторож `linePaths.contract.test.ts` сверяет это значение с CSS-файлом,
 *  чтобы правка толщины в вёрстке не разошлась молча с тем, что проверяет
 *  тест. */
export const LINE_STROKE_WIDTH_VB = 34;

export interface LinePathEntry {
  /** `viewBox = 0 0 1000 vbH` — та же величина, что несёт таблица 4.1. */
  vbH: number;
  /** Путь для холста ≥ 900 px (десктопный рисунок). */
  wide: string;
  /** Путь для мобильной нитки (< 900 px, раздел 8) — прямая на доке. */
  narrow: string;
  /** Вторая заливаемая фигура В ТОЙ ЖЕ коробке — клин первого экрана
   *  (`70-workshop/specs/site-v3/11-line-narrator-brief.md`, раздел 12.4).
   *  Природа та же, что нёс снятый ниже `branch`: второй `<path>` в одном
   *  `<svg>`, раскрывается той же шторкой, второй механики не заводится. В
   *  отличие от `branch` эта фигура не несёт постоянной ширины обводки — она
   *  СТАТИЧЕСКАЯ ЗАЛИВКА (`fill`, не `stroke`), оболочка осевой линии
   *  `hero`, чья полуширина сужается от 50,5 до 17 единиц `viewBox` (ровно
   *  до полуширины штриха — на стыке клин целиком накрывает обводку,
   *  зазора нет по построению). Заполнено только у `hero`; секция без
   *  клина это поле не несёт. Свой сторож — `BackgroundLine.wedge.test.ts`
   *  (раздел 12.8 брифа): `linePaths.contract.test.ts` эту фигуру не
   *  проверяет — `parsePath` не разбирает `Z`, а Г-1/Г-2/Г-4 неприменимы к
   *  фигуре без постоянной толщины обводки. */
  head?: string;
}

/** `branch` — вторая нить в той же коробке секции (`.line-branch`) — БЫЛА
 *  здесь полем `LinePathEntry` (раздел 3 П4 брифа `11-line-narrator-brief.md`,
 *  решение D-125). Прямое указание владельца `2026-08-28`: «убери синюю
 *  линию, которая подчёркивает текст» — отвод `cases` (единственный
 *  оставшийся: пять отводов к цифрам `process` были сняты решением владельца
 *  `2026-08-27`) читался на снимке как случайная линейка — начинался в
 *  воздухе посреди текстовой колонки, шёл в межстрочном просвете поверх
 *  абзаца, не доходил до стрелки внутри карточки и ни одним концом не
 *  примыкал к основной линии.
 *
 *  СНЯТО ЦЕЛИКОМ, а не оставлено пустым необязательным полем: после этой
 *  правки ни одна запись реестра `branch` не несёт, второй нити в системе
 *  не остаётся ни для одной секции — поле, механика раскрытия
 *  (`Section.astro`) и CSS (`.line-branch`, `BackgroundLine.astro`) сняты
 *  вместе, тем же приёмом, каким выше в этом файле снят слой `FALLBACK`
 *  (шапка файла: «второй геометрии в системе не остаётся»). Основная линия
 *  секции `cases` (`wide`) этой правкой не тронута ни на единицу. */

/* ─────────────────────── Рисование (раздел 4.3/4.4/6.2) ─────────────── */

/** Раздел 4.2: левый и правый причал, одинаковы для всех секций. */
const DOCK_LEFT = 59;
const DOCK_RIGHT = 941;
/** Раздел 3, Г-2: вынос концов пути за `viewBox` — торец `round` прячется
 *  под соседний бокс. Тот же приём, что несёт `backgroundLine.ts`. Это
 *  число ставит КООРДИНАТУ конца пути (`M{x},{-OVERHANG}` и симметрично на
 *  выходе), не более — Г-2 читает его как «путь стоит не ближе 60 единиц
 *  к своему видимому боксу», и контракт-тест меряет ровно эту координату.
 *
 *  ДВА ИМЕНОВАННЫХ ИСКЛЮЧЕНИЯ (раздел 12.2, П-А брифа `11-line-narrator-
 *  brief.md`) — Г-2 в части «вертикальный прямой участок ≥ 96» на конце,
 *  где стыка со следующей секцией НЕТ:
 *   1. Верхний конец `hero` — соседа сверху нет, торец уходит выше первой
 *      секции; никакая прокрутка не показывает `y < 0` документа, торца на
 *      экране не существует ни в один момент.
 *   2. Нижний конец `footer` — уход за ЛЕВУЮ кромку холста (`x = −200`)
 *      переехал сюда с `contact` `2026-08-27` (В-4 переоткрыта, раздел 3.3
 *      брифа `16-line-digits-and-finale-brief.md`); конец пути обрезан
 *      `footer .line { clip-path: … }` (`BackgroundLine.astro`) — торца на
 *      экране не существует ни в один момент. **Правки этого абзаца
 *      `2026-08-28`** (раздел 5.1 брифа `18-line-lower-route-brief.md`):
 *      прежняя формулировка ссылалась на `#contact .line { overflow: hidden
 *      }` — такого правила в `BackgroundLine.astro` уже НЕТ, обрезка
 *      переехала на `footer .line` вместе с уходом за левую кромку в тот же
 *      день, но абзац не обновили. Это не архивная неточность: маршрут
 *      варианта А кладёт НАЧАЛО кривой `contact` на 160 единиц ВЫШЕ её
 *      собственной коробки (внутрь `faq`, ради полосы перекрытия, раздел
 *      4.4 брифа `18-…`) — поверь кто-нибудь старой формулировке и верни
 *      `overflow: hidden` на `#contact .line`, полоса перекрытия с `faq`
 *      исчезла бы целиком.
 *  Обоснование Г-2 переписано на попарную проверку полос перекрытия
 *  (раздел 5.1 брифа `18-line-lower-route-brief.md`): переход может
 *  пересекать стык секций (снято решением владельца и координатора, Р-1/
 *  Р-4), при условии что в полосе перекрытия обе краски вертикальны и
 *  стоят на одном `x`. Список именованных исключений сокращён до этих
 *  ДВУХ — там, где стыка нет вовсе; они не про запрет пересечения стыка, а
 *  про то, что полосы перекрытия там не существует по построению. Список
 *  живёт в `linePaths.contract.test.ts`, поимённо. */
const OVERHANG = 60;
/** Раздел 3, Г-2: прямой вертикальный участок на каждом конце пути —
 *  порог 96, здесь взято с запасом 100, чтобы округление кривой не роняло
 *  проверку на десятые доли единицы. */
const STRAIGHT_IN_OUT = 100;

function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

/** ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/15-line-through-scale-
 *  brief.md`, раздел 2.3): здесь раньше жила таблица «шторки»
 *  (`RevealStop[]`, `revealKeyframes`/`simplifyReveal`) и её вынос
 *  (`CAP_OVERHANG`/`CAP_OVERHANG_HERO`/`overhangPercent`) — механика,
 *  считавшая, КАК посекционная шторка едет ПО ДЛИНЕ ДУГИ внутри своего
 *  окна `cover`, и НАСКОЛЬКО она обязана выступать за бокс секции, чтобы
 *  накрыть закрашенный `stroke-linecap: round`-полукруг на конце соседнего
 *  пути. Сквозной шторке (`BackgroundLine.astro`, `position: fixed`, одна
 *  на всю страницу) ни то, ни другое не нужно: окна раскрытия больше нет —
 *  фиксированная шторка режет путь ровно там, где путь пересекает
 *  экранную линию головы, и это по определению та же точка дуги, ради
 *  которой таблица считалась; а поскольку шторка накрывает ВСЁ, что ниже
 *  головы, по построению (а не по размеру бокса своей секции), ей не нужен
 *  вынос под чужой круглый торец. `buildEntry` теперь просто собирает три
 *  геометрических поля записи, без второго прохода по кривой. */
function buildEntry(vbH: number, wide: string, narrow: string): LinePathEntry {
  return { vbH, wide, narrow };
}

/** Прогон — прямая на доке `dockX` (раздел 4.3: «прямая» — секция
 *  спокойная, боковой ход 0). */
function straightPath(vbH: number, dockX: number): string {
  return `M${dockX},${-OVERHANG} L${dockX},${round2(vbH + OVERHANG)}`;
}

/** Мобильная нитка — всегда левый док, вся высота секции (раздел 8). */
function narrowPath(vbH: number): string {
  return straightPath(vbH, DOCK_LEFT);
}

/** Траверс (раздел 4.4): вертикальный вход `margin` (прямая `L`, а не
 *  затухающая кривизна кубики — раздел 12: «на этом линия ломалась уже
 *  дважды», прямой хвост гарантирует Г-2 безусловно, не приближённо), затем
 *  ОДНА симметричная кубика с вертикальной касательной на обоих концах
 *  (оба контрольных узла лежат на высоте середины хода `mid` — классическая
 *  S-кривая без стыков) — ровно та конструкция, для которой раздел 4.4
 *  считает оценку радиуса `R = 3/8 · dy² / dx`, и затем снова вертикальный
 *  выход `margin`. `xFrom` → `xTo` — доки входа и выхода. `margin` по
 *  умолчанию `STRAIGHT_IN_OUT` (порог Г-2 96 плюс запас). Используется
 *  только там, где событие — симметричная S-дуга (`services`); события с
 *  несимметричным входом/выходом (`pricing`, `cases`, `hero`, `contact`)
 *  несут собственную рукописную строку `d` — см. `HAND_DRAWN` ниже. */
function traversePath(vbH: number, xFrom: number, xTo: number, margin = STRAIGHT_IN_OUT): string {
  const yA = margin;
  const yB = round2(vbH - margin);
  const mid = round2((yA + yB) / 2);
  return (
    `M${xFrom},${-OVERHANG} L${xFrom},${yA} ` +
    `C${xFrom},${mid} ${xTo},${mid} ${xTo},${yB} ` +
    `L${xTo},${round2(vbH + OVERHANG)}`
  );
}

const vbHOf = (id: string) => computeVbH(MEASURED_SECTION_HEIGHT[id]);

/** Клин первого экрана — заливка, а не обводка (раздел 12.4 брифа
 *  `11-line-narrator-brief.md`). `<path>` не умеет сужающегося пера в
 *  обводке; поднять `stroke-width` до 101 без сужения потребовало бы
 *  `R ≥ 808` на всём `hero` (Г-1), а путь даёт 397 — недостижимо. Вторая
 *  заливаемая фигура В ТОЙ ЖЕ коробке `hero`, оболочка ОСЕВОЙ линии `hero`,
 *  полуширина которой падает от 50,5 vb (`vbY −84,1`) до 17 vb (`vbY 300` —
 *  ровно полуширина штриха, `LINE_STROKE_WIDTH_VB / 2`) по профилю
 *  `1 − smoothstep(u)` (нулевая производная на обоих концах — на стыке
 *  кромка клина касается кромки штриха, а не подходит под углом, отсюда
 *  «без излома»). 46 точек, стык на `vbY = 300`, числа сняты оболочкой
 *  осевой `hero.wide` по этому профилю, 22 шага по длине дуги — готовая
 *  строка, руками не пересчитывается при правке `hero.wide` (сторож клина,
 *  `BackgroundLine.wedge.test.ts`, проверяет геометрические СЛЕДСТВИЯ
 *  профиля численно, не байтовое совпадение с этой строкой). */
const HERO_HEAD_D =
  'M896.6,-84.1 L883,-60.8 L868,-38.3 L851.4,-16.5 L833.5,4.5 L814.1,24.6 L793.5,44 ' +
  'L771.7,62.7 L748.8,80.6 L725,97.8 L700.4,114.4 L675.1,130.4 L649.2,145.8 ' +
  'L622.8,160.8 L596,175.4 L568.8,189.6 L541.4,203.5 L513.8,217.2 L486,230.8 ' +
  'L458.1,244.2 L430.2,257.7 L402.2,271.2 L374.2,284.9 L389.2,315.4 L416.8,302.3 ' +
  'L444.8,290.1 L473.2,278.5 L501.8,267.4 L530.6,256.6 L559.7,246 L588.8,235.5 ' +
  'L618.1,224.8 L647.3,213.9 L676.6,202.5 L705.9,190.4 L735,177.6 L764,163.7 ' +
  'L792.6,148.5 L820.8,132 L848.4,113.8 L875.1,93.8 L900.7,71.8 L924.9,47.7 ' +
  'L947.3,21.7 L967.5,-6.2 L985.4,-35.9 Z';

/** Десять нарисованных путей (раздел 10, шаг 4) — все секции главной,
 *  реестр полон и не несёт одиннадцатой (`footer`, В-4, см. шапку файла).
 *
 *  ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/11-line-narrator-
 *  brief.md`, раздел 12) переписывает `wide` у ВСЕХ десяти записей —
 *  таблица 12.5 брифа снята заново с живой сборки той же процедурой, что
 *  использует сторож (`flattenPath`/`resampleByLength`/`minRadius`/
 *  `verticalRunLength`/`overhang`/`polylineLength`/`lateralPercent`/
 *  `lateralTurningPoints`), и переносится сюда буквально, а не пересказом.
 *
 *  ПОРЯДОК РАБОТ — раздел 10.14 («полоса отвода», Р-6) исполнен ДО этой
 *  правки: он меняет реальную высоту `cases` на 1180 px (`backgroundLine.ts`,
 *  `MEASURED_SECTION_HEIGHT.cases`, переизмерена ПОСЛЕ Р-6 — 2203, было
 *  2242), а значит и её `vbH`. Таблица 12.5 брифа записана для `vbH = 1900`
 *  (снято на `c0aecbc`, ДО Р-6) — раздел 12.5 брифа, «Порядок работ», даёт
 *  ровно этот случай: `vbY` пути `cases` переносятся не байт-в-байт, а
 *  ОДНИМ УМНОЖЕНИЕМ на отношение новой и старой `vbH`.
 *
 *  РАСХОЖДЕНИЕ С ЗАМЕРОМ БРИФА (доложено, не подогнано молча): факт сборки
 *  этого дня даёт `MEASURED_SECTION_HEIGHT.cases = 2203` (не «≈2203» из
 *  раздела 10.14.6 брифа, а измеренное число, `getBoundingClientRect()` на
 *  1180×900, `reducedMotion: 'reduce'`) — и это число НЕ устарело и не
 *  правится (уточнение `2026-08-28`, раздел 3.1 брифа `18-line-lower-route-
 *  brief.md`): устарело УТВЕРЖДЕНИЕ о нём, не само число. `cases` — ПОКА
 *  единственная секция реестра, чья РЕАЛЬНАЯ высота зависит от ширины
 *  окна: 2203 на 1180, но 2241,73 на 1440 (расхождение 1,76%, вторая
 *  таблица `MEASURED_SECTION_HEIGHT_1440.cases = 2242`, `backgroundLine.
 *  ts`). Записанное здесь число — замер РОВНО на 1180, потому что `vbH`
 *  выводится единственно из 1180 (`CONTAINER_REF`, `backgroundLine.ts`);
 *  заменить его на 2241,7 значило бы сломать и `vbH`, и сторож. Значит
 *  `vbH` этой секции (в отличие от девяти остальных секций главной, где
 *  высота на 1440 и 1180 совпадает буквально) описывает её раскладку НА
 *  1180 и с ошибкой 1,76% — раскладку на 1440. → `computeVbH(2203) = 1867`
 *  (`Math.round(2203000 / 1180) = Math.round(1866,949…) = 1867` — сторож
 *  `linePaths.reveal.test.ts` поймал первый черновой пересчёт этой правки на
 *  `1868`: конец пути должен был лечь на `translate = 100` ровно, а ложился
 *  на `100,05`; починен пересчёт, не тест — см. ниже), не `1900`. Коэффициент
 *  масштаба `1867/1900 ≈ 0,982632`. Поскольку кубическая
 *  кривая — линейная комбинация контрольных точек, умножение ВСЕХ `y`
 *  контрольных и опорных точек на один и тот же коэффициент (при неизменных
 *  `x`) даёт НОВУЮ кривую, чья `y(t)` в любой точке параметра `t` равна
 *  СТАРОЙ `y(t)`, умноженной на тот же коэффициент, — то есть точка на
 *  старой кривой переносится на новую точным правилом `(x, y·k)`, не
 *  приближённо. Этим правилом пересчитаны все `y` основного пути `cases`
 *  (96/428/760/1282/1804) И её отвода (318/376/457) — числа даны как доли
 *  `vbH` в разделе 12.5 брифа ровно для того, чтобы пересчёт был одним
 *  умножением; `x`-координаты (доки 240/59/610, ось отвода 316/470/693) не
 *  масштабируются — они не зависят от высоты бокса. */
const HAND_DRAWN: Readonly<Record<string, LinePathEntry>> = {
  // hero — раздел 12.5 брифа `11-line-narrator-brief.md`: вход у ПРАВОЙ
  // кромки холста (`x = 941`, `y = −60` — исключение Г-2, кривизна на
  // самом старте уже не нулевая, соседа сверху нет), пологая диагональ
  // вниз-влево через правый край заголовка (`x = 901` на верхней кромке
  // секции, `x = 382` на `vbY 300`, `x = 131` на `vbY 500` — сверено с
  // самой строкой `d`), приход на левый док на `vbY 651` — РОВНО на верхней
  // кромке кнопки (`vbY 654`, буквальное совпадение координат «линия дошла
  // до кнопки — кнопка загорается», Р-3), дальше прямая по доку до
  // `vbY 868`. Направление сохранено от референса, порядок кривизны — нет
  // (раздел 12.3, Н-4): при `R ≥ 272` тот же путь из той же точки в ту же
  // строится только «полого сверху, затем плавно на док», а не «круто,
  // потом полого, потом резкий крюк», как на снимке владельца.
  //
  // Клин (`head`, раздел 12.4) — вторая заливаемая фигура в этой же
  // коробке (см. `HERO_HEAD_D`).
  hero: (() => {
    const h = vbHOf('hero');
    return {
      ...buildEntry(h, 'M941,-60 C740,310 59,200 59,768 L59,868', narrowPath(h)),
      head: HERO_HEAD_D,
    };
  })(),
  // pain — раздел 4.3 брифа `05-line`: «прямая», секция ниже порога Г-3 —
  // событие не помещается; продолжает левый док, на котором закончился
  // `hero`. Геометрия не менялась правкой раздела 12 (высота секции не
  // затронута ни Р-6, ни переизмерением шага 0).
  pain: (() => {
    const h = vbHOf('pain');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // services — раздел 12.5 брифа `11-line-narrator-brief.md`: траверс СЛЕВА
  // НАПРАВО сквозь сетку карточек услуг — событие референса (К-1)
  // возвращается сюда. Раздел 12 (второй заход) переписывает реестр заново
  // по размеченному референсу, независимо от исследовательской ветки Р-2
  // (раздел 10.4/10.5 старой редакции — «событие снято отсюда и переехало в
  // pricing»): та правка НИКОГДА не вливалась в `main` (раздел 12.1 брифа,
  // «откатывать нечего»), и в НОВОМ реестре события стоят в ОБЕИХ секциях —
  // здесь симметричный траверс через сетку карточек, в `pricing` ниже —
  // отдельное, несимметричное пересечение карточки-акцента. Симметричная
  // S-дуга (`traversePath`) — та самая конструкция раздела 4.4, вертикаль
  // 160 на входе и выходе.
  services: (() => {
    const h = vbHOf('services');
    return buildEntry(h, traversePath(h, DOCK_LEFT, DOCK_RIGHT), narrowPath(h));
  })(),
  // pricing — раздел 12.5 брифа: пересечение карточки-акцента СПРАВА
  // НАЛЕВО — вход через ПРАВУЮ кромку `#pricing .top-grid > .card--accent`
  // на `vbY 518` под 65°, выход через ЛЕВУЮ на `vbY 675` под 44°, хорда
  // 299 vb (1440) — краска средней линии физически проходит сквозь
  // карточку «Корпоративный сайт» (приёмка П-47). Второе, самостоятельное
  // событие страницы, не «переехавшее» откуда-то (см. комментарий у
  // `services` выше): кривая асимметрична (контрольные точки на разной
  // высоте, `690`/`350`) — не симметричный `traversePath`, а рукописная
  // строка ровно под это событие. Выходит на левый док (`x = 240`),
  // продолжающий приход `cases`.
  pricing: (() => {
    const h = vbHOf('pricing');
    return buildEntry(h, 'M941,-60 L941,96 C920,690 260,350 240,1062 L240,1218', narrowPath(h));
  })(),
  // cases — раздел 12.5 брифа: вход на `x = 240` (продолжает выход
  // `pricing`), подъём вправо к вершине изгиба, лист «Замера» пересечён
  // (вход через ЛЕВУЮ кромку, выход через НИЖНЮЮ), спуск на левый док
  // (`x = 59`, продолжает вход `process`) — краска ныряет под макет
  // телефона и выходит слева от него (карта перекрытия, `linePaths.g5.
  // test.ts`).
  //
  // Числа `y` ПЕРЕСЧИТАНЫ из таблицы 12.5 брифа (записана для `vbH = 1900`,
  // снята ДО раздела 10.14/Р-6) на фактический `vbH` этой секции ПОСЛЕ Р-6
  // — см. врезку перед `HAND_DRAWN` про порядок работ и расхождение с
  // замером брифа. Коэффициент `k = vbHOf('cases') / 1900`:
  //   96 → 96k, 428 → 428k (дважды, оба контрольных узла первой кривой),
  //   760 → 760k (вершина изгиба), 1282 → 1282k (дважды), 1804 → 1804k.
  // При `vbH = 1867` (факт сборки, см. врезку) это даёт: 94.33 / 420.57
  // (дважды) / 746.8 (вершина) / 1259.73 (дважды) / 1772.67.
  //
  // ПРАВКА `2026-08-28`: отвод (`branch`) к стрелке иллюстрации «Замера»
  // СНЯТ решением владельца («убери синюю линию, которая подчёркивает
  // текст») — читался на снимке как случайная линейка, начинался в воздухе
  // посреди текстовой колонки, шёл в межстрочном просвете поверх абзаца, не
  // доходил до стрелки и ни одним концом не примыкал к основной линии. Был:
  // `branch: 'M316,312.48 C470,312.48 693,312.48 693,320 L693,442'` — рождался
  // на самой высокой точке основной кривой (`x = 316`), шёл вдоль верхней
  // кромки стрелки, спускался на её ось (`x = 693`). См. поле `branch` в
  // комментарии над `LinePathEntry` — снято целиком, второй нити в реестре
  // не остаётся. Основная линия (`wide` ниже) этой правкой НЕ тронута ни на
  // единицу.
  cases: (() => {
    const h = vbHOf('cases');
    const wide =
      'M240,-60 L240,94.33 C240,420.57 610,420.57 610,746.8 ' +
      'C610,1259.73 59,1259.73 59,1772.67 L59,1927';
    return buildEntry(h, wide, narrowPath(h));
  })(),
  // process — раздел 12.5 брифа, Н-4/В-1 (правка 2026-08-27): основная
  // линия — прямая по ЛЕВОМУ доку по всей высоте, боковой ход 0. Владелец
  // подтвердил дословно: «основная линия должна проходить РЯДОМ с
  // цифрами» — красное на снимке процесса было желаемым, широкая сиреневая
  // S через середину («траверс справа налево», прежняя запись) была
  // прочитана неверно и отменяется целиком, а не дополняется.
  //
  // ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/16-line-digits-and-
  // finale-brief.md`, раздел 1.3): поле `branch` — пять коротких отводов к
  // цифрам 01…05 — СНЯТО целиком, решением владельца («не рисуем линии к
  // цифрам… когда линия достигает цифры она загорается синим, до этого
  // цифра серая»). Запись становится чистым доком без второго пути; цифра
  // зажигается сама (`Process.astro`, `.num::after`).
  process: (() => {
    const h = vbHOf('process');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // guarantees — раздел 12.1 брифа, В-3: остаётся спокойной. Правило 6.2
  // брифа `05-line` («переход туда не ставится») подтверждено, а не
  // отменено — владелец делегировал решение, а не отменил довод.
  guarantees: (() => {
    const h = vbHOf('guarantees');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // about — ПЕРЕПИСАНО `2026-08-28` (раздел 4 брифа `18-line-lower-route-
  // brief.md`, вариант А, владелец: «линия проходит сквозь мою фотку в
  // разделе обо мне, затем уходит в правую часть страницы»). Отменяет
  // прежний довод раздела 12.3 («Г-2+Г-4 запрещают событие здесь») —
  // отменяет не арифметику, а её ПРИМЕНЕНИЕ: Г-2 в части «переход не
  // пересекает стык» СНЯТА (Р-4, раздел 5.1 брифа `18-…`), и переход
  // отныне не обязан помещаться внутри одной коробки `about` — он вправе
  // дорисовываться поверх соседних `guarantees`/`faq` (сквозная шторка,
  // `overflow: visible`, шапка этого файла и раздел 5.1 брифа `18-…`).
  // Правый переход ПРИНАДЛЕЖИТ `about` (раздел 4.1 брифа: фотография живёт
  // здесь, масштаб перехода обязан идти вместе с высотой ЭТОЙ коробки, не
  // соседней). Вход сквозь фотографию сверху, выход справа, докатка на
  // правый причал глубоко в `faq` (вертикаль на `vbY 700…796`, за пределами
  // собственной коробки `about`, `vbH=564`). Числа сняты числовым поиском
  // под коробку `#about .photo-frame` (П-1 брифа: длина хорды внутри рамки
  // ≥260 px, углубление от кромки ≥80 px, вход сверху/выход справа) при
  // `R_min ≥ 272`; замер — `R_min` 297,9, локальный `|dx/dy|` 3,46 (самая
  // быстрая краска маршрута, задаёт порог новой Г-3 — раздел 5.2 брифа).
  about: (() => {
    const h = vbHOf('about');
    return buildEntry(h, 'M59,-260 L59,-150 C59,445 941,105 941,700 L941,796', narrowPath(h));
  })(),
  // reviews — раздел 4.9 брифа `14-reviews-brief.md`: секция стоит между
  // `about` и `faq`, внутри того же акта 3, на той же стороне (`left`),
  // `turn: 'none'`. Прямой прогон по тому же доводу, что у `about`/`faq`
  // (Г-2+Г-4): секция короткая и текстовая, событию здесь не место.
  // Заводится БЕЗУСЛОВНО — при пустом `REVIEWS` у записи просто нет
  // потребителя (`Section.astro` не рисует эту секцию, пока её нет в
  // `HOME_SECTIONS`), тем же приёмом, каким этот же файл уже держит путь
  // без потребителя (см. шапку файла, «Появится одиннадцатая секция…»).
  reviews: (() => {
    const h = vbHOf('reviews');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // faq — ПЕРЕПИСАНО `2026-08-28` (раздел 4/4.2 брифа `18-line-lower-route-
  // brief.md`). Прежний довод раздела 12.3 («потолок бокового хода в
  // запретной середине Г-4») больше не применяется тем же основанием, что
  // у `about`: снятие Г-2 в части перехода через стык позволяет ходу
  // `about` дотянуть до правого причала здесь, а не внутри собственной
  // коробки. `faq` сам остаётся спокойным — прямая на ПРАВОМ доке
  // (владелец: «в разделе faq линия идёт с правой стороны»), но НЕ во всю
  // высоту коробки: прямая кончается на `vbY 476`, на 70 vb выше низа
  // `vbH=546`, а не на `vbH+OVERHANG`, как у прочих прямых (раздел 4.2
  // брифа). Причина — кривая `contact` отрывается от `x=941` внутри `faq`
  // (её локальная `−64`, что на 73,5 px (1440) / 75,5 px (1180) выше стыка
  // `faq`/`contact`); если бы прямая шла ниже точки отрыва, ниже неё стояли
  // бы ДВЕ краски разом (уходящая кривая и оставшийся обрубок) — читалось
  // бы как вилка. 476 выбрано по 1440, где `faq` короче: там прямая
  // кончается ровно в точке отрыва, на 1180 — на 7 px выше неё. На обеих
  // ширинах вилки нет; полоса перекрытия с `contact` — 110,1 px (1440) и
  // 106,1 px (1180) при пороге 60 (Г-2, раздел 5.1 брифа).
  faq: (() => {
    const h = vbHOf('faq');
    return buildEntry(h, 'M941,136 L941,476', narrowPath(h));
  })(),
  // contact — ПЕРЕПИСАНО `2026-08-28` (раздел 4/6 брифа `18-line-lower-
  // route-brief.md`, владелец: «линия пересекает форму и уходит в левую
  // часть сайта»). Прежняя запись 2026-08-27 (чистый док, боковой ход 0,
  // ниже) отменяется этим же решением: уход влево вернулся сюда как
  // пересечение формы, `footer`-запись ниже (уход за левую кромку холста)
  // не тронута ни на единицу — это разные, не спорящие решения. Начало
  // (`vbY −160…−64`) стоит на 160 единиц ВЫШЕ собственной коробки — оно
  // лежит внутри `faq` (см. правку `2026-08-28` там же и в шапке файла про
  // ошибочный `overflow: hidden`), это и даёт полосу перекрытия с `faq`.
  // Кривая подобрана числовым поиском по семейству `M941,y₀ L941,y₁
  // C941,c₁ 59,c₂ 59,y₂ L59,900` (раздел 6.2 брифа): отсев `R_min ≥ 285`,
  // `|dx/dy| ≤ 3,5`, монотонность по `y`, вертикаль 96 на каждом конце,
  // вынос ≥ 60; из выживших кандидатов измерено на живой странице обеих
  // ширин пересечение с `#contact form` — отбирались только те, что дают
  // РОВНО четыре видимых куска на ОБЕИХ ширинах, каждый ≥ 36 px (приёмка
  // П-5 брифа требует ≥ 30 px). Замер этой кривой: видимые куски 41,0 ·
  // 89,0 · 130,3 · 42,0 px (1440) и 38,2 · 78,0 · 109,1 · 60,7 px (1180);
  // `R_min` 294,3, локальный `|dx/dy|` 2,40 — обе внутри новых Г-1/Г-3.
  contact: (() => {
    const h = vbHOf('contact');
    return buildEntry(h, 'M941,-160 L941,-64 C941,444 59,316 59,800 L59,900', narrowPath(h));
  })(),
  // footer — ВОЗВРАЩЕНА решением владельца `2026-08-27` (раздел 3.3 брифа
  // `16-line-digits-and-finale-brief.md`, вариант Б «Разгон»; снята была
  // разделом 12.1 брифа `11-line-narrator-brief.md`, В-4). Боковой ход
  // 259 vb (`x: 59 → -200`) = 25,9% холста — Г-4 «событие», ТО ЖЕ число,
  // что нёс уход `contact` раньше (тождественно, как и требует раздел 3.3
  // брифа), сохранено буквально при перерисовке кривой ниже.
  //
  // ГЕОМЕТРИЯ КРИВОЙ ПЕРЕРИСОВАНА (не взята дословно строкой из брифа):
  // буквальная строка раздела 3.3 брифа ('M59,-60 L59,80 C59,150 -40,215
  // -200,250') даёт боковой ход 259 vb в вертикальном бюджете всего 170 vb
  // (от начала кривой до её конца) — при таком отношении минимальный
  // радиус кривой (посчитан контракт-тестом, `minRadius`) даёт 74 vb против
  // требуемых Г-1 `8·34=272` — то есть штрих 34 vb был бы согнут почти
  // втрое туже нормы прочих девяти путей, с риском видимого пережатия
  // обводки на самом стыке «прямая → кривая». Подвал — самый короткий бокс
  // реестра (`vbH=245`, вдвое короче следующего по краткости `faq`=574),
  // и тот же двадцатипятипроцентный боковой ход, что легко умещался в
  // высоком боксе `contact` (`vbH` был 818), в подвале требует другого
  // распределения кривизны: бо́льшая часть поворота перенесена в НЕВИДИМЫЙ
  // хвост кривой (после пересечения `x=0`, где `footer .line { clip-path:
  // … }` уже прячет краску — тот же приём, что у `#contact` до правки),
  // а видимая часть (от начала кривой до пересечения) сделана положе.
  // Подобрано числовым поиском (сохранены: боковой ход 259, вертикальный
  // вход ≥96, пересечение `x=0` внутри бокса, `vbH=245`), проверено
  // контракт-тестом: `R_min = 307,7` (≥272 ✓, раздел 3 брифа `05-line`,
  // Г-1), вертикальный вход `102,5` (≥96 ✓), пересечение `x=0` на `vbY ≈
  // 228,1` → документ на ~20 px выше низа страницы (в допуске П-Ф-О2,
  // ожидание раздела 3.3 брифа — 36 px ±40 px).
  //
  // Конец пути (`vbY 380`, вынесен за `vbH=245` в невидимую, обрезанную по
  // X часть) НЕ несёт вертикального прямого участка на конце — исключение
  // Г-2, П-А2, ПЕРЕЕХАВШЕЕ с `contact` на `footer` тем же обоснованием:
  // соседа снизу нет, конец обрезан по X, а не по Y — торца на экране не
  // существует ни в один момент. Вынос за viewBox на конце (`380-245=135`)
  // при этом САМ проходит порог 60 без исключения — только вертикальный
  // прямой участок нуждается в именованном исключении
  // (`linePaths.contract.test.ts`, `NAMED_VERTICAL_END_EXCEPTIONS`).
  // `vbH = computeVbH(MEASURED_FOOTER_HEIGHT) = 245`.
  footer: (() => {
    const h = computeVbH(MEASURED_FOOTER_HEIGHT);
    return buildEntry(h, 'M59,-60 L59,40 C59,205 -60,325 -200,380', narrowPath(h));
  })(),
};

export const LINE_PATHS: Readonly<Record<string, LinePathEntry>> = {
  ...HAND_DRAWN,
};
