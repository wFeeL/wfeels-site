/** Реестр путей линии на фоне главной — бриф `70-workshop/specs/site-v3/
 *  05-line.md`, раздел 10, шаг 4: «Нарисовать одиннадцать путей по
 *  назначению 4.3, шаблону 4.4 и карте перекрытия 6.2. Рисуются руками,
 *  по одному, каждый — со снимком своей секции». Раздел 10, шаг 6:
 *  «Подключить реестр к странице» — `Section.astro` и `Footer.astro`
 *  читают `LINE_PATHS` напрямую, второго источника геометрии в кодовой
 *  базе больше нет (`backgroundLine.ts` считает только сторону/переход,
 *  метаданные Ч-4, не `d`).
 *
 *  До этой правки файл нёс ВТОРОЙ слой, `FALLBACK`, — механический
 *  пересчёт геометрии шага 2 для секций, ещё не нарисованных вручную.
 *  Он снят: `HAND_DRAWN` ниже покрывает все десять секций главной
 *  (раздел 10 шаг 4 завершён полностью), второй геометрии в системе не
 *  остаётся ни для одной записи. Появится одиннадцатая секция без своего
 *  пути — `LINE_PATHS[id]` будет `undefined`, и потребитель
 *  (`Section.astro`) обязан молча пропустить рисунок для неё, а не читать
 *  чужую геометрию (раздел 8: «секция без данных рисует прямую нитку в
 *  левом поле» — отдельная работа, не эта правка).
 *
 *  ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/11-line-narrator-brief.md`,
 *  раздел 12.1, В-4): запись `footer` СНЯТА. Владелец подтвердил референс —
 *  линия уходит за ЛЕВУЮ кромку холста в `contact` (`x = −200`) и в подвал
 *  не заходит; «дотягивание линии до подвала» (прежнее решение, ради
 *  которого нёсся `LINE_PATHS.footer`) отменяется. `Footer.astro` больше не
 *  рисует `.line`/`.line-curtain`; фон подвала (`footer::before`, D-126)
 *  остаётся в силе — это разные решения, второе не отменяется первым.
 *  Реестр несёт РОВНО десять записей — по одной на каждую секцию главной,
 *  без одиннадцатой.
 *
 *  Помощники (`straightPath`, `traversePath`) — не общая формула вместо
 *  рисунка, а инструмент того же рода, что нёс прежний `mirrorD` в
 *  `backgroundLine.ts`: каждый вызов — решение, куда идёт путь ИМЕННО этой
 *  секции; числа (доки, амплитуда) подобраны рукой под карту перекрытия
 *  конкретной секции и проверены контракт-тестом плюс снимком на живой
 *  странице, а не выведены единой формулой на всех.
 *
 *  `narrow` — мобильная нитка (< 900 px, раздел 8): всегда левый док, без
 *  событий — раздел 8 «секция без данных рисует прямую нитку в левом поле»,
 *  тот же приём здесь для КАЖДОЙ секции: мобильная ширина ниже порога 900 px
 *  (раздел 6), на котором вообще стоит рисунок, событий там нет по Г-4
 *  («промежуточных значений нет»). Пока не подключена к разметке (раздел 10
 *  шаг 6 подключает только `wide`, порог видимости остаётся прежним, 480 px)
 *  — отдельная работа шага 6 плана, не эта правка.
 */
import { computeVbH, MEASURED_SECTION_HEIGHT } from './backgroundLine';
import { flattenPath, resampleByLength } from './pathGeometry';

/** Толщина штриха линии в единицах `viewBox` (раздел 7.2 брифа `05-line`):
 *  `stroke-width: 34` в `BackgroundLine.astro`. Единственный источник числа
 *  для контракт-теста (Г-1: `R_min ≥ 8·w`) — второй раз оно не набрано;
 *  сторож `linePaths.contract.test.ts` сверяет это значение с CSS-файлом,
 *  чтобы правка толщины в вёрстке не разошлась молча с тем, что проверяет
 *  тест. */
export const LINE_STROKE_WIDTH_VB = 34;

/** Один keyframe-стоп раскладки «шторки» (`.line-curtain`, `BackgroundLine.
 *  astro`) — задача «пролагивает при листании» (D-080, 2026-08-21), механика
 *  композитной замены `stroke-dashoffset`. `percent` — прогресс анимации
 *  (0…100), тот же самый прогресс `view()`-таймлайна, что раньше двигал
 *  `stroke-dashoffset` линейно по ДЛИНЕ дуги. `translate` — на сколько
 *  процентов СВОЕЙ высоты (`vbH + 2·OVERHANG`, раздел 7.1 брифа `05-line`)
 *  шторка сдвинута вниз: 0 — перекрывает путь целиком (ничего не нарисовано),
 *  100 — ушла полностью (путь открыт целиком). Поле называется и хранится
 *  по-прежнему в процентах `translate` — таблица не переписывалась вторым
 *  проходом D-080 (2026-08-21): в CSS (`BackgroundLine.astro`) это число
 *  теперь выводится не как `translate: 0 X%`, а как безразмерный
 *  `transform: scaleY(1 − X/100)` — тождественная перезапись без новых
 *  чисел, подробности у `toScaleY` в `BackgroundLine.astro`. */
export interface RevealStop {
  percent: number;
  translate: number;
}

export interface LinePathEntry {
  /** `viewBox = 0 0 1000 vbH` — та же величина, что несёт таблица 4.1. */
  vbH: number;
  /** Путь для холста ≥ 900 px (десктопный рисунок). */
  wide: string;
  /** Путь для мобильной нитки (< 900 px, раздел 8) — прямая на доке. */
  narrow: string;
  /** Keyframe-стопы шторки для `wide` (см. `RevealStop`) — уже упрощены
   *  (Дуглас–Пекер, см. `simplifyReveal`), готовы к записи в CSS
   *  `@keyframes` без второй обработки. */
  reveal: RevealStop[];
  /** `CAP_OVERHANG / vbH · 100` — на сколько процентов ВЫСОТЫ СЕКЦИИ
   *  шторка выступает за бокс сверху и снизу (`BackgroundLine.astro`,
   *  `.line-curtain`, `top`/`height`). `CAP_OVERHANG = OVERHANG + w/2`, НЕ
   *  голый `OVERHANG`: шторка обязана накрыть не только координату конца
   *  пути (`-OVERHANG … vbH+OVERHANG`), но и закрашенный `stroke-linecap:
   *  round`-полукруг вокруг неё — иначе он торчит из-под соседней шторки
   *  (дефект «оторванный кусок линии», ревью 2026-08-21, разобран у
   *  `CAP_OVERHANG` в `linePaths.ts`).
   *
   *  ИСКЛЮЧЕНИЕ — `hero` (`70-workshop/specs/site-v3/11-line-narrator-
   *  brief.md`, раздел 12.4): клин первого экрана поднимается выше, чем
   *  накрывает голый `CAP_OVERHANG`, и шторка `hero` обязана мерить свой
   *  вынос от полуширины ГОЛОВЫ, не штриха — см. `CAP_OVERHANG_HERO`
   *  ниже и её описание. */
  overhangPercent: number;
  /** Волосяная вторая нить в ТОЙ ЖЕ коробке (раздел 3 П4 брифа
   *  `70-workshop/specs/site-v3/11-line-narrator-brief.md`, решение
   *  D-125): «коробка секции может нести второй `<path class="line-
   *  branch">`». Раскрывается ТОЙ ЖЕ шторкой, что и `wide` (`reveal`/
   *  `overhangPercent` выше не дублируются под неё): шторка закрывает
   *  содержимое коробки целиком, второй путь внутри неё дорисовывается
   *  синхронно без единой новой строки механики. Необязательное поле —
   *  секция без ветви его просто не несёт. На главной их две: `process`
   *  (пять коротких отводов к цифрам 01…05) и `cases` (один отвод к
   *  стрелке иллюстрации «Замера», раздел 12.5/12.6 брифа выше). */
  branch?: string;
  /** Вторая заливаемая фигура В ТОЙ ЖЕ коробке — клин первого экрана
   *  (`70-workshop/specs/site-v3/11-line-narrator-brief.md`, раздел 12.4).
   *  Природа та же, что у `branch`: второй `<path>` в одном `<svg>`,
   *  раскрывается той же шторкой, второй механики не заводится. В отличие
   *  от `branch` эта фигура не несёт постоянной ширины обводки — она
   *  СТАТИЧЕСКАЯ ЗАЛИВКА (`fill`, не `stroke`), оболочка осевой линии
   *  `hero`, чья полуширина сужается от 50,5 до 17 единиц `viewBox` (ровно
   *  до полуширины штриха — на стыке клин целиком накрывает обводку,
   *  зазора нет по построению). Заполнено только у `hero`; секция без
   *  клина это поле не несёт. Свой сторож — `BackgroundLine.wedge.test.ts`
   *  (раздел 12.8 брифа): `linePaths.contract.test.ts` эту фигуру не
   *  проверяет — `parsePath` не разбирает `Z`, а Г-1/Г-2/Г-4 неприменимы к
   *  фигуре без постоянной толщины обводки. */
  head?: string;
}

/* ─────────────────────── Рисование (раздел 4.3/4.4/6.2) ─────────────── */

/** Раздел 4.2: левый и правый причал, одинаковы для всех секций. */
const DOCK_LEFT = 59;
const DOCK_RIGHT = 941;
/** Раздел 3, Г-2: вынос концов пути за `viewBox` — торец `round` прячется
 *  под соседний бокс. Тот же приём, что несёт `backgroundLine.ts`. Это
 *  число ставит КООРДИНАТУ конца пути (`M{x},{-OVERHANG}` и симметрично на
 *  выходе), не более — Г-2 читает его как «путь стоит не ближе 60 единиц
 *  к своему видимому боксу», и контракт-тест меряет ровно эту координату.
 *
 *  ДВА ИМЕНОВАННЫХ ИСКЛЮЧЕНИЯ (раздел 12.2, П-А брифа `11-line-narrator-
 *  brief.md`) — Г-2 в части «вертикальный прямой участок ≥ 96» на конце,
 *  где стыка со следующей секцией НЕТ:
 *   1. Верхний конец `hero` — соседа сверху нет, торец уходит выше первой
 *      секции и накрывается шторкой (её вынос — `CAP_OVERHANG_HERO` ниже).
 *   2. Нижний конец `contact` — после В-4 подвал линии не несёт, соседа
 *      снизу нет; конец пути лежит за ЛЕВОЙ кромкой холста (`x = −200`) и
 *      обрезан `#contact .line { overflow: hidden }` (`BackgroundLine.
 *      astro`) — торца на экране не существует ни в один момент.
 *  Обоснование Г-2 («круглый торец прячется под соседа», «кривизна на
 *  стыке равна нулю по построению» — и третья причина, `preserveAspectRatio
 *  ="none"` растягивает соседние секции по РАЗНОЙ высоте, поэтому только
 *  вертикаль инвариантна к стыку) на самом СТЫКЕ не ослабляется никогда —
 *  исключения только там, где стыка нет вовсе. Список исключений живёт в
 *  `linePaths.contract.test.ts`, поимённо, а не общим смягчением порога. */
const OVERHANG = 60;
/** Дефект «оторванный кусок линии» (ревью 2026-08-21, разбор задачи «шторка
 *  соседних секций едет не синхронно»): у КООРДИНАТЫ конца пути (`OVERHANG`
 *  выше) есть покрашенный `stroke-linecap: round`, и его КРУГ выходит за эту
 *  координату ещё на `stroke-width / 2` — половину толщины штриха, в
 *  направлении ОТ видимого бокса. Шторка (её `top`/`height`, раздел 7.1
 *  брифа `05-line`) до правки использовала тот же `OVERHANG`, что и
 *  координата пути, — то есть накрывала домен `[-OVERHANG, vbH+OVERHANG]`,
 *  а реально закрашенная часть штриха доходит до `[-OVERHANG-w/2,
 *  vbH+OVERHANG+w/2]`. Полоса шириной `w/2` (17 единиц `viewBox` при
 *  `w = 34` — на экране те самые «~18 px высотой», что измерило ревью)
 *  оставалась некрытой СВОЕЙ шторкой; она не была видна там, где путь один
 *  на секцию (нечему было её обнажить), но все `.line`/`.line-curtain`
 *  делят ОДИН стековый контекст на `z-index: -1` (раздел 6.1 брифа) и
 *  красятся в порядке DOM — путь СЛЕДУЮЩЕЙ секции (позже в DOM) кроет своим
 *  некрытым полукругом шторку ПРЕДЫДУЩЕЙ, даже когда та ещё не домоталась
 *  до конца. Отсюда «оторванный кусок»: чужой некрытый полукруг поверх
 *  чужой же ещё закрытой шторки.
 *
 *  Лечение — шторка (её бокс И таблица `reveal`, откуда берётся `scaleY`)
 *  обязана мерить СВОЙ домен от `CAP_OVERHANG`, не от `OVERHANG`: она
 *  прячет не только координату конца пути, но и весь закрашенный круг
 *  вокруг неё. Координата `d` (сам путь, `OVERHANG`) не трогается — это
 *  число отвечает только за Г-2 и вертикальный стык, к покраске штриха
 *  отношения не имеет. */
const CAP_OVERHANG = OVERHANG + LINE_STROKE_WIDTH_VB / 2;
/** Клин первого экрана поднимается до `vbY = −84,1` (раздел 12.4 брифа
 *  `11-line-narrator-brief.md`) — выше, чем накрывает голый `CAP_OVERHANG`
 *  (`−77`). Для `hero`, и ТОЛЬКО для него, вынос шторки считается от
 *  полуширины ГОЛОВЫ, а не штриха: `HEAD_WIDTH_VB` — ширина клина у самого
 *  широкого места вдвое (`101 = 2·50,5`, замер референса, раздел 12.4).
 *  Число — не подбор: `77 < 84,1` не накрыл бы клин, а следующая круглая
 *  ступень вверх — сама полуширина головы. */
const HEAD_WIDTH_VB = 101;
const CAP_OVERHANG_HERO = OVERHANG + HEAD_WIDTH_VB / 2; // 60 + 50,5 = 110,5
/** Раздел 3, Г-2: прямой вертикальный участок на каждом конце пути —
 *  порог 96, здесь взято с запасом 100, чтобы округление кривой не роняло
 *  проверку на десятые доли единицы. */
const STRAIGHT_IN_OUT = 100;

function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

/* ─────────────────── Шторка (задача «пролагивает», D-080) ─────────────────
 *
 * `stroke-dashoffset` не композитится Chromium — каждый кадр перерисовывает
 * и растрирует весь бокс `.line` заново (замер владельца, `DECISIONS.md`
 * D-080). Композируемая замена: путь нарисован ЦЕЛИКОМ и статически, а
 * открывается шторкой (`.line-curtain`, непрозрачный див того же цвета, что
 * фон за линией — раздел 6.1 брифа `05-line`: линия красится строго между
 * фоном `body`/`footer` и всеми в потоке идущими поверхностями, значит цвет
 * ЗА линией везде один и тот же плоский `--bg`/микс подвала, вторая правда
 * фона не нужна), которую двигает `transform: scaleY(1 − X/100)` (`X` — та
 * же величина, что несёт `translate` ниже) на ТОМ ЖЕ `view()`-таймлайне —
 * ПРАВКА 2026-08-21, второй проход того же дня: первая версия двигала
 * `translate: 0 X%`, но `%` здесь считается от высоты ЭЛЕМЕНТА, а не
 * безразмерно, и такую величину компоузер разрешить сам не может — замер в
 * настоящем браузере (`headed.mjs`) держал 38 пропущенных кадров вместо
 * цели ≤10. `scaleY` — безразмерный множитель, компоузер считает его сам,
 * без обращения к главному потоку; тождество точное, новых чисел нет
 * (`BackgroundLine.astro`, `toScaleY`).
 *
 * Раньше `stroke-dashoffset` открывал путь ЛИНЕЙНО ПО ДЛИНЕ ДУГИ (`pathLength
 * = 1`, `dasharray: 1 1`) — для прямых прогонов это то же самое, что линейно
 * по Y, а для траверсов (S-кривая) — НЕТ: дуга тратит часть длины на
 * горизонтальный ход, и точка, до которой дошла дуга при прогрессе `p`,
 * стоит по Y НИЖЕ, чем `p · vbH`. Чтобы картинка на КАЖДОМ шаге прокрутки
 * осталась той же самой (жёсткое условие D-080), а не только по краям
 * диапазона, шторка не просто едет линейно 0→100% — её путь по Y считается
 * ЧИСЛЕННО по ТОЙ ЖЕ геометрии, что несёт сам путь: `resampleByLength`
 * (`lib/pathGeometry.ts`, тот же инструмент, что и контракт-тест раздела 10
 * шаг 3) даёт точки, равномерные по ДЛИНЕ дуги — i-я точка при `N` точках
 * это ровно то положение, в которое старый `stroke-dashoffset` привёл бы
 * кончик при прогрессе `i/(N-1)`. Для прямых путей (шесть из десяти)
 * таблица выходит линейной автоматически — точки на прямой равномерны по Y
 * и по длине дуги одновременно, второй ветки «прямая/кривая» здесь не
 * заводится: один и тот же код проходит через оба случая. */

/** Количество точек ПЕРЕД упрощением — с запасом на кривизну траверсов
 *  (S-дуга, раздел 4.4): реже — рискуем смазать пик кривизны, чаще —
 *  не изменит результат после `simplifyReveal`, только время сборки. */
const REVEAL_SAMPLE_COUNT = 41;
/** Допуск упрощения Дугласа–Пекера, в тех же единицах, что и оси таблицы
 *  (проценты по обеим осям) — 0,15 % высоты секции на секции ~1000…2000 px
 *  это 1,5…3 px, значительно меньше толщины штриха (31…48 px, раздел 5.1):
 *  зрительно неотличимо от точной кривой, а для прямых путей сводит
 *  таблицу ровно к двум точкам (0 % и 100 %) — CSS не растёт лишним весом
 *  там, где кривизны нет вовсе. */
const REVEAL_SIMPLIFY_TOLERANCE = 0.15;

/** Расстояние точки `p` до отрезка `a→b` в той же двумерной системе
 *  (percent, translate) — обычная проекция на отрезок с зажимом `t∈[0,1]`. */
function pointToSegmentDistance(p: RevealStop, a: RevealStop, b: RevealStop): number {
  const dx = b.percent - a.percent;
  const dy = b.translate - a.translate;
  const len2 = dx * dx + dy * dy;
  if (len2 === 0) return Math.hypot(p.percent - a.percent, p.translate - a.translate);
  const t = Math.max(0, Math.min(1, ((p.percent - a.percent) * dx + (p.translate - a.translate) * dy) / len2));
  const px = a.percent + t * dx;
  const py = a.translate + t * dy;
  return Math.hypot(p.percent - px, p.translate - py);
}

/** Дуглас–Пекер по двум осям (percent, translate) — держит только те стопы,
 *  без которых кривая отклонилась бы больше допуска. Прямая линия сводится
 *  к двум точкам; траверс — к горстке точек вокруг S-дуги. Меньше CSS, тот
 *  же зрительный результат (раздел 13 брифа `05-line`: вес — не вкус). */
function simplifyReveal(points: readonly RevealStop[], tolerance: number): RevealStop[] {
  if (points.length <= 2) return [...points];
  let maxDist = 0;
  let splitAt = 0;
  const first = points[0];
  const last = points[points.length - 1];
  for (let i = 1; i < points.length - 1; i++) {
    const d = pointToSegmentDistance(points[i], first, last);
    if (d > maxDist) {
      maxDist = d;
      splitAt = i;
    }
  }
  if (maxDist > tolerance) {
    const left = simplifyReveal(points.slice(0, splitAt + 1), tolerance);
    const right = simplifyReveal(points.slice(splitAt), tolerance);
    return [...left.slice(0, -1), ...right];
  }
  return [first, last];
}

/** Строит таблицу шторки для одного пути — раздел выше. Домен ЭТОЙ таблицы
 *  (`span`, `translate`) остаётся на `OVERHANG` — намеренно НЕ на
 *  `CAP_OVERHANG`. Таблица кодирует ДВИЖЕНИЕ кончика по ДЛИНЕ дуги пути (то,
 *  что раньше двигал `stroke-dashoffset` линейно), а путь по-прежнему стоит
 *  на координатах `∓OVERHANG` (раздел выше) — трогать этот домен значило бы
 *  либо ломать «прямые пути → два стопа» (натуральная формула по
 *  `CAP_OVERHANG` перестаёт быть коллинеарной с (0,0)–(100,100)), либо
 *  подгонять её обратно вручную вторым числом.
 *
 *  Домен ШТОРКИ (`overhangPercent`, `buildEntry` ниже) — другое дело и
 *  меняется на `CAP_OVERHANG` (или `CAP_OVERHANG_HERO` для клина): сама
 *  таблица `translate` домен-агностична — `0` и `100` дают `scaleY(1)`/
 *  `scaleY(0)` при ЛЮБОМ размере бокса (крайние значения шкалы, не
 *  зависящие от того, что стоит в знаменателе), а именно эти два значения и
 *  есть состояние ДО начала окна раскрытия и ПОСЛЕ его конца
 *  (`animation-fill-mode: both`) — то самое состояние, в котором сегодня
 *  стоит соседняя секция, пока не началось ЕЁ собственное окно (дефект
 *  «оторванный кусок линии», ревью 2026-08-21: шторка соседа была ровно в
 *  `scaleY(1)`, но бокс, который она накрывала, был мал на `w/2`).
 *  Расширение бокса (без изменения таблицы) чинит ровно этот, замеренный
 *  случай — оба конца окна раскрытия остаются точными `scaleY(1)`/`scaleY(0)`
 *  над бо́льшим боксом. Внутри самого окна (0 < percent < 100) это даёт
 *  сглаженную и небольшую (≈ `w/2` единиц `viewBox`, доли процента высоты
 *  секции — раздел 5 брифа `05-line`, `w` — 31…48 px, `w/2` — 15…24 px)
 *  задержку кончика у САМОЙ границы видимого бокса; проверено сторожем
 *  `background-line-ink-continuity.spec.ts` — задержка не даёт нового
 *  разрыва ни на одной из проверенных ширин и тем. */
function revealKeyframes(d: string, vbH: number): RevealStop[] {
  const dense = resampleByLength(flattenPath(d), REVEAL_SAMPLE_COUNT);
  const span = vbH + 2 * OVERHANG;
  const raw = dense.map((p, i) => ({
    percent: round2((i / (REVEAL_SAMPLE_COUNT - 1)) * 100),
    translate: round2(((p.y + OVERHANG) / span) * 100),
  }));
  return simplifyReveal(raw, REVEAL_SIMPLIFY_TOLERANCE);
}

/** Единственная точка сборки записи реестра — считает шторку и её вынос в
 *  процентах (раздел выше), не по второму разу на каждый вызов.
 *  `overhangPercent` — от `capOverhang` (по умолчанию `CAP_OVERHANG`, см.
 *  его комментарий), не от `OVERHANG`: шторка обязана закрыть весь
 *  закрашенный полукруг конца штриха, а не только координату, в которой
 *  путь формально кончается. `hero` — единственный вызов, передающий
 *  `CAP_OVERHANG_HERO` вместо умолчания (раздел 12.4 брифа
 *  `11-line-narrator-brief.md`: вынос шторки первого экрана считается от
 *  полуширины клина, а не штриха). */
function buildEntry(
  vbH: number,
  wide: string,
  narrow: string,
  capOverhang: number = CAP_OVERHANG,
): LinePathEntry {
  return {
    vbH,
    wide,
    narrow,
    reveal: revealKeyframes(wide, vbH),
    overhangPercent: round2((capOverhang / vbH) * 100),
  };
}

/** Прогон — прямая на доке `dockX` (раздел 4.3: «прямая» — секция
 *  спокойная, боковой ход 0). */
function straightPath(vbH: number, dockX: number): string {
  return `M${dockX},${-OVERHANG} L${dockX},${round2(vbH + OVERHANG)}`;
}

/** Мобильная нитка — всегда левый док, вся высота секции (раздел 8). */
function narrowPath(vbH: number): string {
  return straightPath(vbH, DOCK_LEFT);
}

/** Траверс (раздел 4.4): вертикальный вход `margin` (прямая `L`, а не
 *  затухающая кривизна кубики — раздел 12: «на этом линия ломалась уже
 *  дважды», прямой хвост гарантирует Г-2 безусловно, не приближённо), затем
 *  ОДНА симметричная кубика с вертикальной касательной на обоих концах
 *  (оба контрольных узла лежат на высоте середины хода `mid` — классическая
 *  S-кривая без стыков) — ровно та конструкция, для которой раздел 4.4
 *  считает оценку радиуса `R = 3/8 · dy² / dx`, и затем снова вертикальный
 *  выход `margin`. `xFrom` → `xTo` — доки входа и выхода. `margin` по
 *  умолчанию `STRAIGHT_IN_OUT` (порог Г-2 96 плюс запас). Используется
 *  только там, где событие — симметричная S-дуга (`services`); события с
 *  несимметричным входом/выходом (`pricing`, `cases`, `hero`, `contact`)
 *  несут собственную рукописную строку `d` — см. `HAND_DRAWN` ниже. */
function traversePath(vbH: number, xFrom: number, xTo: number, margin = STRAIGHT_IN_OUT): string {
  const yA = margin;
  const yB = round2(vbH - margin);
  const mid = round2((yA + yB) / 2);
  return (
    `M${xFrom},${-OVERHANG} L${xFrom},${yA} ` +
    `C${xFrom},${mid} ${xTo},${mid} ${xTo},${yB} ` +
    `L${xTo},${round2(vbH + OVERHANG)}`
  );
}

const vbHOf = (id: string) => computeVbH(MEASURED_SECTION_HEIGHT[id]);

/** Клин первого экрана — заливка, а не обводка (раздел 12.4 брифа
 *  `11-line-narrator-brief.md`). `<path>` не умеет сужающегося пера в
 *  обводке; поднять `stroke-width` до 101 без сужения потребовало бы
 *  `R ≥ 808` на всём `hero` (Г-1), а путь даёт 397 — недостижимо. Вторая
 *  заливаемая фигура В ТОЙ ЖЕ коробке `hero`, оболочка ОСЕВОЙ линии `hero`,
 *  полуширина которой падает от 50,5 vb (`vbY −84,1`) до 17 vb (`vbY 300` —
 *  ровно полуширина штриха, `LINE_STROKE_WIDTH_VB / 2`) по профилю
 *  `1 − smoothstep(u)` (нулевая производная на обоих концах — на стыке
 *  кромка клина касается кромки штриха, а не подходит под углом, отсюда
 *  «без излома»). 46 точек, стык на `vbY = 300`, числа сняты оболочкой
 *  осевой `hero.wide` по этому профилю, 22 шага по длине дуги — готовая
 *  строка, руками не пересчитывается при правке `hero.wide` (сторож клина,
 *  `BackgroundLine.wedge.test.ts`, проверяет геометрические СЛЕДСТВИЯ
 *  профиля численно, не байтовое совпадение с этой строкой). */
const HERO_HEAD_D =
  'M896.6,-84.1 L883,-60.8 L868,-38.3 L851.4,-16.5 L833.5,4.5 L814.1,24.6 L793.5,44 ' +
  'L771.7,62.7 L748.8,80.6 L725,97.8 L700.4,114.4 L675.1,130.4 L649.2,145.8 ' +
  'L622.8,160.8 L596,175.4 L568.8,189.6 L541.4,203.5 L513.8,217.2 L486,230.8 ' +
  'L458.1,244.2 L430.2,257.7 L402.2,271.2 L374.2,284.9 L389.2,315.4 L416.8,302.3 ' +
  'L444.8,290.1 L473.2,278.5 L501.8,267.4 L530.6,256.6 L559.7,246 L588.8,235.5 ' +
  'L618.1,224.8 L647.3,213.9 L676.6,202.5 L705.9,190.4 L735,177.6 L764,163.7 ' +
  'L792.6,148.5 L820.8,132 L848.4,113.8 L875.1,93.8 L900.7,71.8 L924.9,47.7 ' +
  'L947.3,21.7 L967.5,-6.2 L985.4,-35.9 Z';

/** Десять нарисованных путей (раздел 10, шаг 4) — все секции главной,
 *  реестр полон и не несёт одиннадцатой (`footer`, В-4, см. шапку файла).
 *
 *  ПРАВКА `2026-08-27` (`70-workshop/specs/site-v3/11-line-narrator-
 *  brief.md`, раздел 12) переписывает `wide` у ВСЕХ десяти записей —
 *  таблица 12.5 брифа снята заново с живой сборки той же процедурой, что
 *  использует сторож (`flattenPath`/`resampleByLength`/`minRadius`/
 *  `verticalRunLength`/`overhang`/`polylineLength`/`lateralPercent`/
 *  `lateralTurningPoints`), и переносится сюда буквально, а не пересказом.
 *
 *  ПОРЯДОК РАБОТ — раздел 10.14 («полоса отвода», Р-6) исполнен ДО этой
 *  правки: он меняет реальную высоту `cases` на 1180 px (`backgroundLine.ts`,
 *  `MEASURED_SECTION_HEIGHT.cases`, переизмерена ПОСЛЕ Р-6 — 2203, было
 *  2242), а значит и её `vbH`. Таблица 12.5 брифа записана для `vbH = 1900`
 *  (снято на `c0aecbc`, ДО Р-6) — раздел 12.5 брифа, «Порядок работ», даёт
 *  ровно этот случай: `vbY` пути `cases` переносятся не байт-в-байт, а
 *  ОДНИМ УМНОЖЕНИЕМ на отношение новой и старой `vbH`.
 *
 *  РАСХОЖДЕНИЕ С ЗАМЕРОМ БРИФА (доложено, не подогнано молча): факт сборки
 *  этого дня даёт `MEASURED_SECTION_HEIGHT.cases = 2203` (не «≈2203» из
 *  раздела 10.14.6 брифа, а измеренное число, `getBoundingClientRect()` на
 *  1180×900, `reducedMotion: 'reduce'`) → `computeVbH(2203) = 1867`
 *  (`Math.round(2203000 / 1180) = Math.round(1866,949…) = 1867` — сторож
 *  `linePaths.reveal.test.ts` поймал первый черновой пересчёт этой правки на
 *  `1868`: конец пути должен был лечь на `translate = 100` ровно, а ложился
 *  на `100,05`; починен пересчёт, не тест — см. ниже), не `1900`. Коэффициент
 *  масштаба `1867/1900 ≈ 0,982632`. Поскольку кубическая
 *  кривая — линейная комбинация контрольных точек, умножение ВСЕХ `y`
 *  контрольных и опорных точек на один и тот же коэффициент (при неизменных
 *  `x`) даёт НОВУЮ кривую, чья `y(t)` в любой точке параметра `t` равна
 *  СТАРОЙ `y(t)`, умноженной на тот же коэффициент, — то есть точка на
 *  старой кривой переносится на новую точным правилом `(x, y·k)`, не
 *  приближённо. Этим правилом пересчитаны все `y` основного пути `cases`
 *  (96/428/760/1282/1804) И её отвода (318/376/457) — числа даны как доли
 *  `vbH` в разделе 12.5 брифа ровно для того, чтобы пересчёт был одним
 *  умножением; `x`-координаты (доки 240/59/610, ось отвода 316/470/693) не
 *  масштабируются — они не зависят от высоты бокса. */
const HAND_DRAWN: Readonly<Record<string, LinePathEntry>> = {
  // hero — раздел 12.5 брифа `11-line-narrator-brief.md`: вход у ПРАВОЙ
  // кромки холста (`x = 941`, `y = −60` — исключение Г-2, кривизна на
  // самом старте уже не нулевая, соседа сверху нет), пологая диагональ
  // вниз-влево через правый край заголовка (`x = 901` на верхней кромке
  // секции, `x = 382` на `vbY 300`, `x = 131` на `vbY 500` — сверено с
  // самой строкой `d`), приход на левый док на `vbY 651` — РОВНО на верхней
  // кромке кнопки (`vbY 654`, буквальное совпадение координат «линия дошла
  // до кнопки — кнопка загорается», Р-3), дальше прямая по доку до
  // `vbY 868`. Направление сохранено от референса, порядок кривизны — нет
  // (раздел 12.3, Н-4): при `R ≥ 272` тот же путь из той же точки в ту же
  // строится только «полого сверху, затем плавно на док», а не «круто,
  // потом полого, потом резкий крюк», как на снимке владельца.
  //
  // Клин (`head`, раздел 12.4) — вторая заливаемая фигура в этой же
  // коробке, `HEAD_WIDTH_VB`/`CAP_OVERHANG_HERO` выше.
  hero: (() => {
    const h = vbHOf('hero');
    return {
      ...buildEntry(
        h,
        'M941,-60 C740,310 59,200 59,768 L59,868',
        narrowPath(h),
        CAP_OVERHANG_HERO,
      ),
      head: HERO_HEAD_D,
    };
  })(),
  // pain — раздел 4.3 брифа `05-line`: «прямая», секция ниже порога Г-3 —
  // событие не помещается; продолжает левый док, на котором закончился
  // `hero`. Геометрия не менялась правкой раздела 12 (высота секции не
  // затронута ни Р-6, ни переизмерением шага 0).
  pain: (() => {
    const h = vbHOf('pain');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // services — раздел 12.5 брифа `11-line-narrator-brief.md`: траверс СЛЕВА
  // НАПРАВО сквозь сетку карточек услуг — событие референса (К-1)
  // возвращается сюда. Раздел 12 (второй заход) переписывает реестр заново
  // по размеченному референсу, независимо от исследовательской ветки Р-2
  // (раздел 10.4/10.5 старой редакции — «событие снято отсюда и переехало в
  // pricing»): та правка НИКОГДА не вливалась в `main` (раздел 12.1 брифа,
  // «откатывать нечего»), и в НОВОМ реестре события стоят в ОБЕИХ секциях —
  // здесь симметричный траверс через сетку карточек, в `pricing` ниже —
  // отдельное, несимметричное пересечение карточки-акцента. Симметричная
  // S-дуга (`traversePath`) — та самая конструкция раздела 4.4, вертикаль
  // 160 на входе и выходе.
  services: (() => {
    const h = vbHOf('services');
    return buildEntry(h, traversePath(h, DOCK_LEFT, DOCK_RIGHT), narrowPath(h));
  })(),
  // pricing — раздел 12.5 брифа: пересечение карточки-акцента СПРАВА
  // НАЛЕВО — вход через ПРАВУЮ кромку `#pricing .top-grid > .card--accent`
  // на `vbY 518` под 65°, выход через ЛЕВУЮ на `vbY 675` под 44°, хорда
  // 299 vb (1440) — краска средней линии физически проходит сквозь
  // карточку «Корпоративный сайт» (приёмка П-47). Второе, самостоятельное
  // событие страницы, не «переехавшее» откуда-то (см. комментарий у
  // `services` выше): кривая асимметрична (контрольные точки на разной
  // высоте, `690`/`350`) — не симметричный `traversePath`, а рукописная
  // строка ровно под это событие. Выходит на левый док (`x = 240`),
  // продолжающий приход `cases`.
  pricing: (() => {
    const h = vbHOf('pricing');
    return buildEntry(h, 'M941,-60 L941,96 C920,690 260,350 240,1062 L240,1218', narrowPath(h));
  })(),
  // cases — раздел 12.5 брифа: вход на `x = 240` (продолжает выход
  // `pricing`), подъём вправо к вершине изгиба, лист «Замера» пересечён
  // (вход через ЛЕВУЮ кромку, выход через НИЖНЮЮ), спуск на левый док
  // (`x = 59`, продолжает вход `process`) — краска ныряет под макет
  // телефона и выходит слева от него (карта перекрытия, `linePaths.g5.
  // test.ts`).
  //
  // Числа `y` ПЕРЕСЧИТАНЫ из таблицы 12.5 брифа (записана для `vbH = 1900`,
  // снята ДО раздела 10.14/Р-6) на фактический `vbH` этой секции ПОСЛЕ Р-6
  // — см. врезку перед `HAND_DRAWN` про порядок работ и расхождение с
  // замером брифа. Коэффициент `k = vbHOf('cases') / 1900`:
  //   96 → 96k, 428 → 428k (дважды, оба контрольных узла первой кривой),
  //   760 → 760k (вершина изгиба), 1282 → 1282k (дважды), 1804 → 1804k.
  // При `vbH = 1867` (факт сборки, см. врезку) это даёт: 94.33 / 420.57
  // (дважды) / 746.8 (вершина) / 1259.73 (дважды) / 1772.67.
  //
  // Отвод (`branch`) — рождается ВНУТРИ краски основной линии в её самой
  // высокой точке (`x = 316`, `y = 318k = 312.48` — тот же коэффициент,
  // потому что это точка НА главной кривой, а кубика — линейная комбинация
  // контрольных точек: умножение всех `y` контрольных/опорных точек кривой
  // на `k` переносит любую точку кривой `(x, y)` в `(x, y·k)` точно), идёт
  // вправо вдоль верхней кромки стрелки, поворачивает вниз на её ось
  // (`x = 693`, компромисс между `672,8` на 1440 и `717,6` на 1180 — коробки
  // `.arrow` на этих ширинах не пересекаются, раздел 12.5 брифа, «Про две
  // ширины») до нижней кромки второго отрезка стрелки (`y = 457k = 449.06`).
  cases: (() => {
    const h = vbHOf('cases');
    const wide =
      'M240,-60 L240,94.33 C240,420.57 610,420.57 610,746.8 ' +
      'C610,1259.73 59,1259.73 59,1772.67 L59,1927';
    return {
      ...buildEntry(h, wide, narrowPath(h)),
      branch: 'M316,312.48 C470,312.48 693,312.48 693,369.47 L693,449.06',
    };
  })(),
  // process — раздел 12.5 брифа, Н-4/В-1 (правка 2026-08-27): основная
  // линия — прямая по ЛЕВОМУ доку по всей высоте, боковой ход 0. Владелец
  // подтвердил дословно: «основная линия должна проходить РЯДОМ с
  // цифрами» — красное на снимке процесса было желаемым, широкая сиреневая
  // S через середину («траверс справа налево», прежняя запись) была
  // прочитана неверно и отменяется целиком, а не дополняется.
  //
  // Отвод (`branch`, раздел 12.5 брифа) — пять КОРОТКИХ подпутей вместо
  // прежней одной длинной «спины шагов»: каждый рождается на оси основной
  // линии (`x = 59`), идёт с лёгким наклоном вниз (10 vb — чтобы отвод
  // ПРОРИСОВЫВАЛСЯ, а не появлялся целиком) до `x = 112` — на 11 vb внутри
  // коробки своей цифры (`x` 101…194). `vbY` рождения (253/447/641/811/1005)
  // и прихода (263/457/651/821/1015) — числа брифа, привязка к цифрам
  // 01…05 не зависит от `vbH` секции `process` (её высота не менялась ни
  // Р-6, ни переизмерением шага 0 — секция вне обеих правок).
  process: (() => {
    const h = vbHOf('process');
    return {
      ...buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h)),
      branch:
        'M59,253 L112,263 M59,447 L112,457 M59,641 L112,651 M59,811 L112,821 M59,1005 L112,1015',
    };
  })(),
  // guarantees — раздел 12.1 брифа, В-3: остаётся спокойной. Правило 6.2
  // брифа `05-line` («переход туда не ставится») подтверждено, а не
  // отменено — владелец делегировал решение, а не отменил довод.
  guarantees: (() => {
    const h = vbHOf('guarantees');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // about — раздел 12.3 брифа, Н-2/Н-3: остаётся прямой НЕ по вкусу, а по
  // арифметике Г-2+Г-4. Боковой ход не может быть растянут на две секции
  // (Г-2 требует вертикального входа/выхода внутри ОДНОЙ коробки), и
  // потолок бокового хода этой секции при `R ≥ 272` — 215 vb (21,5% холста)
  // — лежит СТРОГО МЕЖДУ «прямая» (≤2%) и «событие» (≥25%) из Г-4: событие
  // здесь невозможно ни в каком варианте геометрии, а не «дорого». Правило
  // 6.2 брифа `05-line` («два открытых экрана подряд обязаны оставаться
  // спокойными») при этом подтверждено, а не отменено — здесь оно просто
  // не единственная причина.
  about: (() => {
    const h = vbHOf('about');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // faq — раздел 12.3 брифа, тот же довод, что у `about`: потолок бокового
  // хода при `R ≥ 272` — 140 vb (14% холста), тоже строго в запретной
  // середине Г-4. Линия не может пересечь панель вопросов и остаётся на
  // левом доке.
  faq: (() => {
    const h = vbHOf('faq');
    return buildEntry(h, straightPath(h, DOCK_LEFT), narrowPath(h));
  })(),
  // contact — раздел 12.5 брифа: прямая по доку до `vbY 560`, затем уход
  // влево — краска покидает холст (`x = −200`, за левой кромкой) на
  // `vbY 878 = vbH + OVERHANG`, конец пути НЕ несёт вертикального
  // прямого участка (исключение Г-2, П-А2 — соседа снизу нет после В-4,
  // подвал линии не несёт, а сама координата обрезана `#contact .line {
  // overflow: hidden }`). Вертикальный вход держит 622 vb — событие
  // (Г-4, боковой ход 25,9%).
  contact: (() => {
    const h = vbHOf('contact');
    return buildEntry(h, 'M59,-60 L59,560 C59,720 -40,840 -200,878', narrowPath(h));
  })(),
};

export const LINE_PATHS: Readonly<Record<string, LinePathEntry>> = {
  ...HAND_DRAWN,
};
