---
/* Русская главная. Вся разметка — в `components/home/HomePage.astro`, одна
 * на обе языковые версии: у английской (`pages/en/index.astro`) такая же
 * обёртка. Своей строки текста у страницы нет намеренно — иначе две главные
 * начали бы расходиться по одной правке за раз.
 *
 * Язык страницы `HomePage` выводит из адреса, поэтому пропа здесь нет: два
 * места, объявляющих язык одной страницы, — это два места, где он может
 * разойтись. */
import HomePage from '../components/home/HomePage.astro';
---
<HomePage />
