// Секция 6 «Что можно проверить» — единственный источник её содержимого.
// Разметка (`components/home/Proof.astro`) читает отсюда, а не хранит текст
// сама.
//
// Текст — дословно из `70-workshop/specs/site-v3/02-texts.md`, секция 6.
// Числа сравнения сроков (2–4 дня / 2–3 недели / от одного до четырёх
// месяцев) уже сверены с источниками и подтверждены владельцем — не
// пересчитывать и не «уточнять» (02-texts.md, «Что здесь нельзя менять при
// правке»).
//
// Секция РАСТУЩАЯ (02-home.md, раздел 9): в спеке 05 сюда приходят демо-чат
// и живые стенды, ещё два элемента. `Proof.astro` кладёт эти элементы в
// CSS-сетку (`repeat(auto-fit, minmax(...))`), а не в три жёстко заданных
// места — добавление сюда четвёртой и пятой записи не требует переделки
// разметки.
export interface ProofItem {
  /** Жирный заголовок карточки — дословно из 02-texts.md. */
  lead: string;
  /** Один-два абзаца — дословно из 02-texts.md, включая заглушки `[вес]` и
   *  `[кратность]` там, где они есть (см. `WEIGHT_PLACEHOLDER` /
   *  `MULTIPLIER_PLACEHOLDER` ниже). */
  paragraphs: readonly string[];
  linkText?: string;
  linkHref?: string;
}

/** Заглушка веса страницы. Подставляется последним шагом плана, вместе с
 *  `MULTIPLIER_PLACEHOLDER`, из вывода `npm run check:budget` — не раньше,
 *  чем все одиннадцать секций на месте (02-texts.md, «Что здесь нельзя
 *  менять при правке»). Тест на утечку заглушки в боевую сборку —
 *  `src/tests/dist-proof-placeholders.test.ts`, сейчас намеренно `skip`. */
export const WEIGHT_PLACEHOLDER = '[вес]';

/** Заглушка кратности («типичный сайт тяжелее в N раз»), выводится из веса —
 *  подставляется только вместе с ним, см. `WEIGHT_PLACEHOLDER`. */
export const MULTIPLIER_PLACEHOLDER = '[кратность]';

export const PROOF_ITEMS: readonly ProofItem[] = [
  {
    lead: 'Сколько это занимает',
    paragraphs: [
      'Лендинг из шаблона — 2–4 дня, сайт до десяти страниц — 2–3 недели.',
      'Студии публично называют для сайта от одного до четырёх месяцев — ' +
      'это календарь от выбора подрядчика до запуска, вместе с очередью и ' +
      'согласованиями. Мой срок — тоже календарь, и в нём нет очереди, ' +
      'потому что я беру мало проектов.',
    ],
  },
  {
    lead: 'Сколько весит эта страница',
    paragraphs: [
      'Она весит 372 КБ. Типичный сайт в интернете — 2,5 МБ, в семь раз больше.',
      'Разница видна не в цифрах, а в том, сколько ваш клиент ждёт с ' +
      'телефона в зоне слабой связи.',
    ],
    linkText: 'Как это сделано',
    linkHref: '/cases/site-v3',
  },
  {
    lead: 'Форма внизу — та самая',
    paragraphs: [
      'Она не нарисована для красоты. Это ровно то, что входит в «сайт ' +
      'под ключ»: заявка приходит мне в Telegram через несколько секунд, ' +
      'с защитой от спама и без потерь.',
      'Напишите — и увидите, как это работает, ещё до того, как мы ' +
      'договоримся.',
    ],
  },
];

if (PROOF_ITEMS.length < 3) {
  throw new Error('data/proof.ts: секция 6 несёт минимум три доказательства.');
}
