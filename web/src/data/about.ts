// Секция 9 «Обо мне» — единственный источник её содержимого. Разметка
// (`components/home/About.astro`) читает отсюда, а не хранит текст сама.
//
// Оба абзаца (`ABOUT_BLOCKS`) — текст ВЛАДЕЛЬЦА от 2026-08-19, он заменил
// собой утверждённый 2026-08-11 текст секции 9 из `70-workshop/specs/site-v3/
// 02-texts.md`: тот спецификационный текст этой секции больше не описывает.
// Что остаётся в силе из прежнего блока «что нельзя менять при правке»:
//   - прошедшее время («вел», а не «сопровождаю») — отношения закрыты,
//     статус клиента «в паузе» (`30-clients/doggylogy/`);
//   - имя клиента не называется — D-011. «Зоосервис в Москве» единственная
//     допустимая формулировка, и это ЕДИНСТВЕННАЯ секция сайта, где слово
//     «клиент» вообще звучит: в кейсах оно запрещено;
//   - никаких сумм: ни 112 000 ₽, ни любой другой.
// Что из него ОТПАЛО вместе с текстом: «34 оплаченные задачи», «полтора
// года» и объяснение цены «не ниже средней» (D-010) — новый текст владельца
// этих доводов не несёт вовсе.
//
// Тест на отсутствие имени клиента в репозиторий НЕ заводится (02-home-plan.md,
// задача 12, «Про имя клиента»): чтобы проверить отсутствие строки, её
// пришлось бы написать в файле теста, то есть внести имя клиента в
// репозиторий сайта. Вместо этого тест утверждает ПОЛОЖИТЕЛЬНОЕ — что секция
// несёт формулировку «зоосервис в Москве» (см. `ABOUT_CLIENT_LABEL` ниже).

/** Фото из v2 (`50-code/portfolio-site/public/wfeels-photo.jpg`), уменьшено
 *  и переведено в AVIF 2026-08-26: 44,9 КБ JPEG -> 26,0 КБ (качество 70).
 *  Формат приведён к остальной графике сайта — галереи кейсов уже AVIF,
 *  фото оставалось единственным JPEG. Гейт веса считает картинку
 *  независимо от `loading="lazy"`, поэтому 18,9 КБ вернулись прямо в
 *  запас главной, который был 0,9 КБ.
 *  и пересжато под бюджет главной (`sips -Z 760 -s formatOptions 58`):
 *  1086×724 / 137 667 байт → 760×506 / 46 012 байт. Дальше в `public/` этого
 *  репозитория — Astro отдаёт статику из `public/` как есть, без второго
 *  прохода оптимизации. */
import { assertParallel, type Locale } from '../i18n/locales';

export const ABOUT_PHOTO = {
  src: '/wfeels-photo.avif',
  width: 760,
  height: 506,
  alt: 'Даниил за ноутбуком в процессе работы',
};

/** Подпись под фото (была первой строкой в теле секции, брифом
 *  `70-workshop/specs/site-v3/04-sections-brief.md`, раздел 4, уходит под
 *  фотографию мелким текстом — пункт 15 списка правок 2026-08-14).
 *
 *  Формулировка — правка ВЛАДЕЛЬЦА, не эта вёрстка: пункт 15 того же списка
 *  (ответ 2026-08-18, вопрос 1 брифа) заменил утверждённую строку «Три года
 *  … делаю сайты …» на «3 года … сайты и автоматизацию …» — числительное
 *  цифрой, без глагола «делаю». Хвост строки от «для малого бизнеса»
 *  сохранён как в прежней утверждённой версии; правится только начало.
 *  Особый режим текста (раздел 4 брифа) запрещает редактировать факты
 *  самому — это не тот случай: правку внёс владелец, не я. */
export const ABOUT_LEAD =
  'Даниил. 3 года на Python, сайты и автоматизацию для малого бизнеса.';

/** Единственное место сайта, где называется формулировка происхождения
 *  реального бизнеса — без имени (D-011). Тест `dist-home-about-faq-contact.
 *  test.ts` утверждает, что она есть на странице дословно. */
export const ABOUT_CLIENT_LABEL = 'зоосервис в Москве';

/** Два абзаца правой колонки — раньше несли заголовок (`title`), брифом
 *  раздел 4.3 подзаголовки «Полтора года с одним бизнесом» и «Как я
 *  использую ИИ» СНИМАЮТСЯ (пункт 15), и `ABOUT_BLOCKS` становится плоским
 *  упорядоченным списком текстов — мёртвое поле `title` не оставляется.
 *  Порядок сменился: текст про инструменты идёт ПЕРВЫМ, про бизнес —
 *  вторым (тот же пункт 15).
 *
 *  Сами ТЕКСТЫ — правка владельца, заданная пунктом 15 списка правок
 *  2026-08-14 и поставленная сюда 2026-08-19: до этого дня в код попала
 *  только раскладка, а тексты оставались от 2026-08-11, и секция говорила
 *  не то, что владелец решил. Особый режим текста: тексты составлены
 *  владельцем осознанно, править их фактическую часть нельзя. Что это
 *  значит для правки этого файла:
 *    - «Postgres» остаётся, хотя в проекте SQLite: владелец предупреждён и
 *      решил термин сохранить (D-045). Это не опечатка и не недосмотр;
 *    - «Два года» остаётся как написано — не сверяется и не округляется;
 *    - «3 Telegram-бота» цифрой, «Postgres база данных» именно в таком
 *      порядке слов, «полноценный Telegram Mini App» — дословно;
 *    - буквы «ё» в этих двух абзацах нет намеренно: на сайте вместо «ё»
 *      пишется «е» (решение владельца), поэтому «вел», а не «вёл».
 *      Сторож на это стоит в `about.test.ts`.
 *
 *  `{claude}`, `{figma}`, `{chatgpt}` в первом абзаце и `{postgres}`,
 *  `{github}` во втором (правка владельца 2026-08-20) — места под значки марок
 *  (`components/BrandMark.astro`, разбор — `lib/brandMarkers.ts`). Текст
 *  остаётся ОДНИМ литералом со значками внутри, а не режется на массив
 *  кусков: так предложение можно сверить с сообщением владельца глазами.
 *  Сторож в `about.test.ts` сверяет строку без маркеров с утверждённым
 *  предложением посимвольно. */
export const ABOUT_BLOCKS: readonly string[] = [
  'Код пишу при помощи {claude} Claude Code, дизайн составляю в {figma} ' +
    'Figma и переношу на сайт через {chatgpt} ChatGPT. Архитектуру, ' +
    'безопасность и проверку кода оставляю за собой. Именно с помощью ' +
    'таких инструментов я добиваюсь максимальной скорости в разработке ' +
    'продуктов.',
  `Два года вел ${ABOUT_CLIENT_LABEL}: были разработаны 3 Telegram-бота, ` +
    'полноценный Telegram Mini App с онлайн-записью и {postgres} Postgres ' +
    'база данных. Вся работа фиксировалась и сопровождалась при помощи ' +
    '{github} GitHub — любой разработчик сможет изучить контекст изменений ' +
    'и адаптироваться под проект.',
];

/** Закрывающая строка — жирная, тезис всего сайта. */
export const ABOUT_CLOSING =
  'Каждое число на этом сайте — из кода или из замера. Ни одного придуманного.';

/* ─────────────────────────── Английская версия ────────────────────────────
 *
 * Тот же рассказ и те же факты. Ни одно утверждение здесь не усилено и не
 * ослаблено переводом: «3 года на Python» — та же цифра из `10-offer/
 * SKILLS.md`, «два года вёл зоосервис в Москве» — тот же срок и тот же
 * безымянный клиент (D-011: имя не называется), «3 Telegram-бота» — то же
 * число.
 *
 * ГЛАГОЛ «ВЁЛ» — самое опасное место этого перевода, и первый вариант его
 * провалил. Стояло `I ran a pet-care service in Moscow`, и для носителя языка
 * это значит «я управлял зоосервисом», то есть был этим бизнесом, а не его
 * подрядчиком. Русское «вёл» в разделе «Обо мне» читается как «сопровождал
 * как разработчик»; английское `ran` такой двусмысленности не имеет и выбирает
 * ровно то прочтение, которого в базе нет: `10-offer/SKILLS.md` называет
 * зоосервис КЛИЕНТОМ, а не делом владельца. Нашло это ревью выноса наружу
 * 2026-08-22. Правка — `looked after`: по-английски так говорят про чужие
 * системы, за которые отвечаешь («I look after their website»), и роль
 * подрядчика остаётся на месте.
 *
 * ПАССИВ «БЫЛИ РАЗРАБОТАНЫ» сохранён намеренно. Активное `I built 3 Telegram
 * bots` читается сильнее русского оригинала — а перевод не вправе усиливать
 * утверждение о себе, даже когда `SKILLS.md` его подтверждает. Добавлено
 * только `for it`: без него английский пассив висит без адресата.
 *
 * Маркеры `{claude}`, `{figma}`, `{chatgpt}`, `{postgres}`, `{github}` стоят
 * и в переводе: их разбирает `lib/brandMarkers.ts`, и каждый обязан стоять
 * ПЕРЕД словом марки через ровно один пробел — иначе сборка падает. Порядок
 * марок в предложении сохранён, чтобы обе версии называли один и тот же
 * инструмент в одном и том же месте рассказа.
 *
 * Закрывающая строка — самое сильное утверждение секции, и по-английски она
 * такая же короткая: длинный перевод сделал бы из неё оговорку. */
export const ABOUT_PHOTO_ALT_EN = 'Daniil working at his laptop';

export const ABOUT_LEAD_EN =
  'Daniil. 3 years with Python, websites and automation for small businesses.';

export const ABOUT_CLIENT_LABEL_EN = 'a pet-care service in Moscow';

export const ABOUT_BLOCKS_EN: readonly string[] = [
  'I write the code with {claude} Claude Code, put the design together in ' +
    '{figma} Figma and bring it onto the site through {chatgpt} ChatGPT. ' +
    'Architecture, security and code review stay with me. Tools like these ' +
    'are exactly how I get products built as fast as I do.',
  `For two years I looked after ${ABOUT_CLIENT_LABEL_EN}: 3 Telegram bots ` +
    'were built for it, a full Telegram Mini App with online booking, and a {postgres} Postgres ' +
    'database. All of the work was tracked and documented in {github} GitHub — ' +
    'any developer can read the history of the changes and get up to speed on it.',
];

export const ABOUT_CLOSING_EN =
  'Every number on this site comes from code or from a measurement. Not one of them is made up.';

export interface AboutText {
  photoAlt: string;
  lead: string;
  blocks: readonly string[];
  closing: string;
}

const ABOUT_BY_LOCALE: Record<Locale, AboutText> = {
  ru: {
    photoAlt: ABOUT_PHOTO.alt,
    lead: ABOUT_LEAD,
    blocks: ABOUT_BLOCKS,
    closing: ABOUT_CLOSING,
  },
  en: {
    photoAlt: ABOUT_PHOTO_ALT_EN,
    lead: ABOUT_LEAD_EN,
    blocks: ABOUT_BLOCKS_EN,
    closing: ABOUT_CLOSING_EN,
  },
};

assertParallel('data/about.ts', {
  ru: ABOUT_BY_LOCALE.ru.blocks,
  en: ABOUT_BY_LOCALE.en.blocks,
});

export function aboutText(locale: Locale): AboutText {
  return ABOUT_BY_LOCALE[locale];
}

/* Два блока — решение брифа `04-sections-brief.md`, раздел 4, а не «сколько
   получилось». Сторож проверяет оба языка. */
for (const [locale, text] of Object.entries(ABOUT_BY_LOCALE)) {
  if (text.blocks.length !== 2) {
    throw new Error(
      `data/about.ts: секция 9 несёт ровно два блока сверх лида и закрытия (${locale}: ${text.blocks.length}).`,
    );
  }
}
