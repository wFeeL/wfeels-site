// Секция 9 «Обо мне» — единственный источник её содержимого. Разметка
// (`components/home/About.astro`) читает отсюда, а не хранит текст сама.
//
// Текст — дословно из `70-workshop/specs/site-v3/02-texts.md`, секция 9,
// вместе с блоком «что нельзя менять при правке»:
//   - прошедшее время «вёл», а не «сопровождаю» — отношения закрыты,
//     статус клиента «в паузе» (`30-clients/doggylogy/`);
//   - имя клиента не называется — D-011. «Зоосервис в Москве» единственная
//     допустимая формулировка, и это ЕДИНСТВЕННАЯ секция сайта, где слово
//     «клиент» вообще звучит: в кейсах оно запрещено;
//   - суммы 112 000 ₽ здесь нет намеренно — «34 оплаченные задачи» говорит
//     то же самое про регулярность, не подставляя число под сравнение с
//     ценой 70 000 ₽ за сайт;
//   - «не ниже средней» объяснено, а не оправдано (D-010): ускорение уже
//     внутри часов, второй понижающий коэффициент был бы двойным счётом.
//
// Тест на отсутствие имени клиента в репозиторий НЕ заводится (02-home-plan.md,
// задача 12, «Про имя клиента»): чтобы проверить отсутствие строки, её
// пришлось бы написать в файле теста, то есть внести имя клиента в
// репозиторий сайта. Вместо этого тест утверждает ПОЛОЖИТЕЛЬНОЕ — что секция
// несёт формулировку «зоосервис в Москве» (см. `ABOUT_CLIENT_LABEL` ниже).

/** Фото из v2 (`50-code/portfolio-site/public/wfeels-photo.jpg`), уменьшено
 *  и пересжато под бюджет главной (`sips -Z 760 -s formatOptions 58`):
 *  1086×724 / 137 667 байт → 760×506 / 46 012 байт. Дальше в `public/` этого
 *  репозитория — Astro отдаёт статику из `public/` как есть, без второго
 *  прохода оптимизации. */
export const ABOUT_PHOTO = {
  src: '/wfeels-photo.jpg',
  width: 760,
  height: 506,
  alt: 'Даниил за ноутбуком в процессе работы',
};

/** Первая строка секции — жирная, целиком. */
export const ABOUT_LEAD =
  'Даниил. Три года на Python, делаю сайты и автоматизацию для малого бизнеса.';

export interface AboutBlock {
  title: string;
  text: string;
}

/** Единственное место сайта, где называется формулировка происхождения
 *  реального бизнеса — без имени (D-011). Тест `dist-home-about-faq-contact.
 *  test.ts` утверждает, что она есть на странице дословно. */
export const ABOUT_CLIENT_LABEL = 'зоосервис в Москве';

export const ABOUT_BLOCKS: readonly AboutBlock[] = [
  {
    title: 'Полтора года с одним бизнесом',
    text:
      `Полтора года вёл ${ABOUT_CLIENT_LABEL}: три бота, онлайн-запись, 34 ` +
      'оплаченные задачи за год. Это не разовый заказ, а работа, которая ' +
      'продолжалась — и по-моему это говорит больше, чем длинный список ' +
      'логотипов.',
  },
  {
    title: 'Как я использую ИИ',
    text:
      'Код и часть дизайна пишу вместе с ИИ. Архитектуру, безопасность, ' +
      'дизайн-решения и проверку оставляю за собой. Отсюда дни там, где ' +
      'обычно недели. И отсюда же цена не ниже средней: ускорение учтено в ' +
      'сроках, а не вычтено из качества.',
  },
];

/** Закрывающая строка — жирная, тезис всего сайта. */
export const ABOUT_CLOSING =
  'Каждое число на этом сайте — из кода или из замера. Ни одного придуманного.';

if (ABOUT_BLOCKS.length !== 2) {
  throw new Error('data/about.ts: секция 9 несёт ровно два блока сверх лида и закрытия.');
}
